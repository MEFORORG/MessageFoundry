[← Master Test Plan index](../MASTER-TEST-PLAN.md) · *Part II — Subsystem chapters*

---

## 17. Interoperability, Migration, Upgrade & Acceptance (UAT)

**ID prefix:** `MIG` · **Surface:** engine + CLI + infra (docs/CI), with the web console and the VS Code IDE entering at their version seams
· **Primary risk:** the adoption path is a chain of *documented-but-unenforced* constraints — a store-backend switch, an engine upgrade, and a cutover reconcile each have a loud-failure story written in prose and no code or test behind it.

### 17.1 Scope & objectives

This chapter covers the paths an organisation actually walks to *adopt*, *cut over to*, *upgrade*, and *accept* MessageFoundry:

1. **Legacy import.** The deterministic Corepoint action-list importer (`messagefoundry import corepoint`, [`messagefoundry/corepoint_import.py`](../../../messagefoundry/corepoint_import.py), ADR 0086) and the second partner-artifact import path, WSDL (ADR 0122). Mirth has **no deterministic importer** — see §17.9 Q3.
2. **Parallel-run / cutover validation.** The `tee` relay + parity engine ([`tee/`](../../../tee), docs/TEE-RELAY.md, BACKLOG #14) and the parallel-run reconcile harness ([`harness/reconcile/`](../../../harness/reconcile)), plus the primitives they depend on: per-outbound `simulate` ([`config/wiring.py:2551`](../../../messagefoundry/config/wiring.py)) and `deployed=False` placeholders (ADR 0111).
3. **Store migration posture.** SQLite → server-DB is **greenfield-only**; this chapter tests that the constraint is *enforced or loudly surfaced*, not merely written down.
4. **Version upgrade, store schema migration, rollback, and compatibility** of config, store, and the four version seams (engine↔web console, engine↔harness, engine↔IDE connection schema, engine↔adopter config repo).
5. **Adopter CI** — the scaffolded config repo and its generated `check.yml`.
6. **Documentation-accuracy testing** — mechanical doc-executability (fenced blocks load, printed commands exist, pinned versions are real) plus the irreducibly human naive-user walkthrough.
7. **Vendor/partner interoperability** — Epic, Oracle Health, lab, radiology, HIE; HL7 v2 dialect quirks; FHIR server conformance; SMART-secured servers; DICOM SCP/SCU peers.
8. **Timezone / DST / clock-skew and locale**, and the **support/diagnostic workflow**.
9. This area's **release go/no-go contribution**.

**Explicitly out of scope — owned elsewhere, cited not restated:**

| Area | Owner |
|---|---|
| Corepoint import coverage gaps (real-export reconciliation, CLI wrapper, batch-split FileSource, `anonymize-captures` no-body-to-stdout) | [`docs/testing/FEATURE-COVERAGE-PLAN.md`](../FEATURE-COVERAGE-PLAN.md) rows **FCP:CFG-22** (:188, :481, :1406), **FCP:CLI-17** (:185, :478, :1442), **FCP:FILE-9** (:169, :462, :639), **FCP:ANON-9** (:73, :426, :1359) |
| On-box reconcile capability proof under the NSSM service identity | [`docs/testing/WIN2025-TEST-PLAN.md`](../WIN2025-TEST-PLAN.md) **W25:S3.10** (:622-641) |
| Day-1 human-closed box acceptance (AD/Kerberos, TOTP, NSSM lifecycle, no-console-flash, disposition walk, firewall, ACLs) | WIN2025-TEST-PLAN **W25:S1.AC-\*** (:306-322) + Appendix E |
| Box-level sign-off green set | WIN2025-TEST-PLAN **W25:S6.3** (:1193-1200) |
| Staged rollout gates Stage 0→3 | [`docs/EARLY-ADOPTER-GUIDE.md`](../../EARLY-ADOPTER-GUIDE.md) §11 (:561-630) |
| Honest limits of the adopter gate | [`docs/ADOPTER-CI.md`](../../ADOPTER-CI.md) §5 (:136-158) |
| The engine↔console seam contract and bump procedure | [`docs/WEBCONSOLE-PACKAGE.md`](../../WEBCONSOLE-PACKAGE.md) §2-§3 |
| Throughput ceilings, connection-scale, failover timing | [`docs/LOAD-TESTING.md`](../../LOAD-TESTING.md) + `harness/load/` |
| **Database shards** (splitting the store across multiple DBs) | ADR 0039 — **shelved**. The built scaling axis is the **engine shard** (ADR 0037/0063: N `serve --shard` processes over ONE unified store) and is not a migration concern |

**Vocabulary note for this chapter.** `messagefoundry/corepoint_import.py` contains a `Channel` dataclass ([:215](../../../messagefoundry/corepoint_import.py)) — that is the *incumbent's* term for a Corepoint integration unit, used only inside the importer's parse model. Its **output** is a module with a named inbound Connection, an `@router`, and `@handler`s: a graph, not a bundling element. No test in this chapter may assert the existence of a MessageFoundry "channel"/"route" object.

**Cross-document ID prefixes (plan-wide convention).** This plan owns its own gap-ID space (`MIG-06`, `HA-02`, `API-13`, …). `docs/testing/FEATURE-COVERAGE-PLAN.md` and the WIN2025 documents use *colliding* IDs — there is a `CLI-17`, an `HA-20` and an `API-13` in more than one of them. Therefore **every reference to another document's ID carries a prefix**: `FCP:` for a FEATURE-COVERAGE-PLAN row (`FCP:CLI-17`), `W25:` for a WIN2025 test ID (`W25:S3.10`). A bare `MIG-nn` — or any bare ID in this plan's own format — always means **this plan's own row**.

**The publishing boundary (plan-wide convention, owned by this chapter).** This repository is the **public** repo (`github.com/MEFORORG/MessageFoundry`). Part of the evidence this plan cites is real, current and owner-held but deliberately **not published here**. Absence from a public checkout is a **publishing boundary, not a missing artifact** — a reviewer may not record "file not found" against it as a coverage defect, and no row may be weakened, hedged or dropped on that ground.

- **`docs/BACKLOG.md` is a published *baseline* that stops at `## 231.`** ([:6883](../../BACKLOG.md) is the last item heading; the boundary is stated in prose at [:269](../../BACKLOG.md), [:4394](../../BACKLOG.md) and [:6041](../../BACKLOG.md) — *"the file you are reading ends at #231, while #242–#246 and their successors do not appear in it at all … their absence here is a publishing boundary, not evidence of completion"*). The ledger continues past it. **A citation above #231 — #233, #234, #242–#246, #275, #279, #285, #306, #310, #313, #744 and their successors — is sound evidence**; it simply does not resolve inside the public tree. Do not disclaim it, do not call it unverifiable, do not strip it. Where a public-only reader needs the hint, annotate it **once and neutrally** — "(above the published #231 baseline)" — never on every occurrence. In-baseline citations (#14, #105, #124, #131, #149, #180) resolve normally.
- **`docs/security/` (~32 files), `docs/reviews/` and `docs/marketing/` are gitignored post-cutover** ([`.gitignore:144-146`](../../../.gitignore); the rationale — "an attacker roadmap" — is in the same block at :147-149). The AD-federation lab runbook, the off-loopback deployment guide and the ASVS assessment set **exist** for the owner: they are **withheld from the public repo, not absent**. Write "withheld", never "missing" or "does not exist".
- **Therefore** a test row may cite withheld evidence and still be sound. Where a doc-anchored *suite* cannot see its document from a public checkout, the engineering answer is the MIG-29 / MIG-30 treatment — make the skip explicit, censused and CI-answerable — not a claim that the document is gone.
- **What the plan owes the reader** is one statement of which evidence a public-only reader cannot open: everything under those three directories, plus any BACKLOG number above #231. This subsection is that statement; Part I §0.5 carries the same rule for readers who start there. MIG-74's linter enforces it mechanically, classifying a withheld path as `WITHHELD` against a checked-in manifest instead of failing the run.

### 17.2 Already covered — do not re-test

| Evidence | What it proves |
|---|---|
| `tests/test_corepoint_import.py` (56 tests, 992 lines) | ADR 0086 AC-1..AC-6f against `tests/fixtures/corepoint/acme_adt_package.xml`: vocabulary mapping, unmapped→in-place TODO never dropped, `@Disabled` preserved as comment and counted, nested control flow, every-statement-accounted-for, generated module compiles and passes `messagefoundry check`, hostile `@Data` cannot inject code (:981), DTD/entity payload raises `CorepointImportError`, dash→dotted coordinate translation, cross-message write never becomes a `msg.set` |
| `tests/test_corepoint_import.py:945` + `:968` | The `import corepoint` CLI: `--json` ledger pinned at mapped=21 / unmapped=8 / disabled=1 on the fixture, and a malformed export exits 1 with a clean error, never a traceback. *(FEATURE-COVERAGE-PLAN still records **FCP:CLI-17** as `partial` at :478 — see MIG-33.)* |
| `tests/test_lens_parse.py:461` | ADR 0086 AC-4: every emitted `@handler` classifies into typed lens rows with no whole-file refusal |
| `tests/test_reconcile_harness.py` (13) · `test_reconcile_compare.py` (7) · `test_reconcile_capture.py` (2) | Parallel-run normalize + pair + load core: separators read from MSH not hardcoded, MSH-7/MSH-10 blanked, per-connection blank extension, repetition/segment sort only when configured, ignore-segments, alternate separators, non-MSH raises; JSONL/dir/batch loaders; unmatched + unkeyed + duplicate-key findings; `CaptureSink` ACKs and appends JSONL, and records an unparseable message **without** ACKing |
| `tests/test_tee_relay.py` (12 async) | Fan-out with AA on receipt; a Corepoint NAK is logged, not propagated; fail-closed on Corepoint transport failure; copy-listener → MEFOR only; retry-then-succeed; bounded MEFOR queue drops oldest with a log; a store error does not crash the handler |
| `tests/test_tee_compare.py` / `test_tee_correlate.py` / `test_tee_report.py` / `test_tee_hl7_fields.py` / `test_tee_mefor_api.py` / `test_tee_store.py` / `test_tee_mllp.py` / `test_tee_cli.py` | The whole `tee` parity stack: exact/semantic/mismatch classification, hybrid MSH-10 + content-key correlation including A40 merge and ambiguity refusal, PHI-safe summary vs `--show-diffs`-gated diffs, vendored HL7 field addressing, engine-API assembly, NAK log, purge confirmation, `compare` end-to-end |
| `tests/test_anon_parity.py` (10) | `messagefoundry/anon/` and `tee/anon/` stay byte-identical on the shared logic and produce identical output on a golden + adversarial corpus; fail-closed on both sides. **This is the model a vendored-surface guard should follow** |
| `tests/test_timezone.py` (32) | DST-boundary offset differs by the DST hour; precision preserved; fractional seconds verbatim; hour-precision conversion crosses midnight; Eastern↔Central round-trip; embedded offset pins the instant; malformed input raises |
| `tests/test_timer_source.py:295` | Cron `next_after` is DST-aware with an explicit IANA zone — the **only** DST-transition test in the repo |
| `tests/test_logging.py:723-780` + `tests/test_settings.py:350-360` | The startup SNTP clock-sync gate: fail-closed refuses on skew and on an unreachable peer; warn-only still starts; within-threshold starts clean; the validator requires a peer |
| `tests/test_fifo_index_migration.py` (434 lines, backend-parametrized ×3) | ADR 0060 upgrade path: old-named indexes dropped, seq-trailing created, idempotent re-run, crash-atomicity in the DROP→CREATE gap, mixed-version hazard; carries an explicit `_downgrade_to_old_indexes` simulator (:166) that also deletes the ADR 0064 `schema_meta` marker |
| `tests/test_store_schema_hash.py` (6) + `tests/test_sqlserver_schema_init.py` (5) | ADR 0064: the hash is deterministic and moves on ANY DDL edit or `_MIGRATION_REV` bump; the schema applock precedes every CREATE and the FIFO index migration; a marker-current DB skips the batch |
| `tests/test_staged_pipeline.py:797` (+ `:989`) | A pre-staged-pipeline SQLite DB (flat `outbox`, NOT NULL `destination_name`, plaintext bodies) migrates into `queue` with encryption applied; orphaned rows are skipped and logged |
| `tests/test_webconsole_seam_snapshot.py` (3) + `tests/golden/webconsole_seam.snapshot` | The injected `UiDeps`/`CoreHandlers`/`AdminHandlers` contract is snapshotted; the console supports exactly the engine seam; `assert_engine_seam` refuses a neighbouring seam |
| `tests/test_connection_schema.py` (12) + `ide/src/test/suite/connection-schema.test.ts` | `connection schema --json` emits `schemaVersion` and every keyword-only factory parameter of every registered transport; the extension **refuses a newer `schemaVersion` loudly** and accepts an older one (`connectionSchemaModel.ts:147-160`) |
| `tests/test_scaffold.py` (6) + `tests/test_scaffold_requirements.py` (3) | `messagefoundry init` writes the skeleton (including the starter fixture `messages/sets/example_adt.hl7`), refuses a non-empty dir, never clobbers; the scaffolded config passes `check`; the generated `check.yml` is valid YAML with a `verify-engine` job, an `audit-pin` job, and `--require-hashes` guidance |
| `tests/test_checks.py` (38) + `test_checks_lint.py` + `test_checks_handler_security.py` + `test_checks_gate_parity.py` | The adopter gate's validate/dryrun/posture semantics, advisory lint, ADR 0144 handler-security strict mode, gate parity through security keys |
| `tests/test_verify.py` (34) + `tests/test_verify_federation.py` | `verify` host/store/smoke/manual/federation sections; MANUAL rows carry evidence and are never faked PASS; live smoke passes on AA and fails on NAK/unreachable; `--check-disposition` catches post-ACK dead-letters; a missing explicit config path FAILS rather than skipping |
| `tests/test_support_bundle.py` (17) | Bundle members, counts-only config summary, real status models, redacted log tail (non-allowlisted HL7 segment, free-text name/DOB, `MEFOR_` secret, store path basename only), line-count preservation |
| `tests/test_release_pipeline.py` (19) + `tests/test_version.py` (2) | sdist is package-only (the 0.1.0..0.2.15 private-doc leak class), the two leak allowlists cannot drift, PyPI publish is last + tag-gated + tokenless OIDC, jobs gated ON the source repo, releases idempotent and rerunnable, the console tag namespace is disjoint from the engine's, version single-sourced and SemVer |
| `tests/test_install_instruction_provenance.py` (7) | ASVS 15.2.4: every extra named in an install instruction exists in `[project.optional-dependencies]`; no shipped text installs an unpublished distribution by bare name; the unpublished list cannot rot vacuous |
| `tests/test_backlog_status_check.py` (13) + `scripts/docs/backlog_status_check.py` | Every BACKLOG item carries a status banner; shipped/open banners cannot contradict; duplicate numbers rejected; a CHANGELOG "shipped" claim against an open item is flagged; runs against the real `docs/BACKLOG.md` |
| `tests/test_cutover_slug_rot.py` (4) | No self-substituting rewrite; no workflow gates a job off this repo's own slug; present-tense mirror prose ratchets down |
| `tests/test_update_check.py` (14) | ADR 0026 update-available signal: local mode default; `live` mode rejected in settings (:117) |
| `tests/test_cli.py:43` | `messagefoundry --help` is cp1252-encodable — the legacy-Windows codepage regression |
| `.github/workflows/ci.yml` `test` (:41-262) | The whole `tests/` tree on the ubuntu + windows matrix with ruff / ruff-format / mypy-strict |
| `.github/workflows/ci.yml` `windows-service-smoke` (:1081-1272) | NSSM install → start → `/health` → real MLLP send recorded → graceful stop → uninstall on hosted Windows runners — the nearest thing to an executed install walkthrough |
| `.github/workflows/ci.yml` `docker-smoke` (:1273) and `ide` (:263-327) | Slim Linux image smoke; IDE `tsc` + esbuild + node unit tests + headless VS Code integration tests |

**DONE — do not re-plan.** The `tee` parity stack and the `anon` vendoring-drift guard are mature and adversarially tested; add nothing there beyond the CLI-boundary PHI assertion in MIG-43. The ADR 0060 FIFO index migration is the best-tested migration in the repo, including a deliberate downgrade simulator — the vintage matrix (MIG-06..MIG-13) must *extend* its backend-parametrized fixture pattern, not duplicate it. The engine↔console seam **snapshot** gate is correct and complete at the code layer; what is missing is entirely at the *packaging and runbook* layer (MIG-15, MIG-16). `verify`'s section model, MANUAL honesty, and report writers are settled. The `release.yml` leak/idempotence/OIDC gates are settled.

**Two recon corrections that change the plan.** (a) The Corepoint **role layer is wired** — `_parse_statement` calls `parse_roles` / `_role_verb` / `_operands_from_roles` at [`corepoint_import.py:1289-1301`](../../../messagefoundry/corepoint_import.py) and dispatches into `_map_roles` at :1346, which emits real `Action` objects (`copy_field` / `set_field` / `append_to_field`, :1152-1170). `SPEC-105 §0`'s "Vocabulary `Action` objects produced: **0**" is a **pre-wiring measurement** and must not be quoted as current state. (b) `docs/BACKLOG.md` is a **published baseline ending at `## 231.`** ([:6883](../../BACKLOG.md) is the last heading; the boundary prose is at [:6041](../../BACKLOG.md) and [:4394](../../BACKLOG.md)). **#313 sits above that baseline and is sound evidence** — the ledger continues past what this repo publishes, so a row may rest on it; it simply cannot be opened from a public checkout (see the publishing-boundary convention in §17.1). What must not happen is a row treating the baseline as the whole ledger: #105's own entry says as much at [:269](../../BACKLOG.md) — "the real gate is #313, which is invisible from this baseline".

### 17.3 Risk analysis

| Risk | Failure mode | Blast radius | Detected today? | Priority |
|---|---|---|---|---|
| Greenfield-only rule enforced nowhere | An operator flips `[store].type` to `postgres`/`sqlserver`; `open_store` ([`store/base.py:1739`](../../../messagefoundry/store/base.py)) selects the backend with **no prior-backend detection, no drain check, no warning**. The engine starts clean on an empty schema | The populated SQLite store — in-flight ingress/routed/outbound rows plus all retained PHI history and audit — is silently orphaned. Named as "Greenfield-only data loss" at `docs/releases/v0.1-PLAN.md:457`; the only shipped mitigation is prose in 6 documents and a manual checkbox at `docs/DEPLOY-SERVER-DB.md:274` | **No** | **P0** |
| Engine upgrade past a seam bump **bricks the whole engine**, not just `/ui` | `[api].serve_ui` defaults **True** ([`settings.py:672`](../../../messagefoundry/config/settings.py)). `create_app`'s `serve_ui` tail calls `assert_engine_seam(ENGINE_UI_SEAM)` ([`api/app.py:4951`](../../../messagefoundry/api/app.py)) and lets `UiSeamMismatch` propagate out of `create_app`. `SUPPORTED_ENGINE_SEAMS == frozenset({15})` — strict lockstep, no N-1 | `messagefoundry serve` **fails to start** on a PHI box mid-upgrade. Intake stops. The runbooks operators follow (EARLY-ADOPTER-GUIDE §13 step 4 :679, INSTALL-GUIDE §10 :306-313) bump only `messagefoundry==<new>` and never mention the console wheel | Fails loud at startup — but nothing tests the *sequence*, and nothing warns beforehand | **P0** |
| The documented install-time seam defence does not exist | WEBCONSOLE-PACKAGE.md §2 claims a three-layer handshake whose layer 1 is "the PEP 508 range on the engine dependency fails an out-of-range pair at `pip`/`uv` resolve". `packaging/messagefoundry-webconsole/pyproject.toml:38-42` declares a **bare `"messagefoundry"`** with a comment conceding "a bare dep is fine for now" | `pip install -U messagefoundry` resolves happily across a seam bump. The first symptom is the P0 above. A documented control that is absent is worse than a known-absent one: the runbook author trusted it | **No** — nothing binds the doc claim to the metadata | **P0** |
| Reconcile diff output is PHI and **ungated** | `harness/reconcile/report.py` `render_text` prints up to 20 mismatched messages' field-level `left`/`right` **values** by default (:63-66); `render_json` **always** emits them (:31-42). `_run_compare` ([`__main__.py:80-93`](../../../harness/reconcile/__main__.py)) prints unconditionally and writes `--report-json` verbatim | This is the tool an operator runs during a live cutover, and its output is what gets pasted into a ticket. `tee` gates the same class of output behind `--show-diffs` with an explicit warning (`tee/__main__.py:269`, :451-453); the reconcile harness has **no equivalent** | **No** | **P0** |
| Reconcile capture writes PHI bodies by default | `CaptureSink` accepts `anonymizer=` ([`capture.py:46`](../../../harness/reconcile/capture.py)) but `_run_capture` never passes it and the `capture` subparser exposes **no** `--anonymize`/`--salt-env` flag | During a shadow phase the JSONL holds real production bodies on disk, on a non-production box, with no purge story | **No** | **P0** |
| No store upgrade/downgrade vintage matrix | SQLite `_migrate` ([`store/store.py:2933`](../../../messagefoundry/store/store.py)) is ~120 lines of `PRAGMA table_info`-probed idempotent ALTERs with **no schema version number**. It is exercised only from a fresh DB plus one legacy-`outbox` fixture. No test opens a DB written by an *older released engine*; none opens a *newer* DB with an older engine | ADR 0060 is the precedent: a re-key that "silently never landed on upgraded DBs" and shipped green. Both EARLY-ADOPTER-GUIDE §13 and INSTALL-GUIDE §10 prescribe pin-back rollback as the primary lever, against a store the newer engine has already opened, with nothing behind it | **No** | **P0** |
| Adopter install docs pin a three-releases-stale version | `docs/INSTALL-GUIDE.md:55`, `:61`, `:144`; `docs/USER-GUIDE.md:97`, `:113`; `docs/EARLY-ADOPTER-GUIDE.md:153`, `:156`; `docs/MENTAL-MODEL.md:343` all say `messagefoundry==0.1.0` while `messagefoundry/__init__.py` is **`0.3.2`** | `docs/SUPPORT-POLICY.md:9` supports only the latest release with no back-ports, on a 15-day critical / 30-day high SLA. A naive user lands on an unsupported build and their scaffolded `audit-pin` job reds on day one. INSTALL-GUIDE:144 additionally *misdescribes* the scaffold, which pins `__version__` correctly (`scaffold.py:370`) | **No** — `test_install_instruction_provenance.py` checks extras and distribution names, never versions | **P1** |
| No doc-executability tests over the adopter guides | `tests/test_off_loopback_runbook.py:179` / `:218` / `:89` is the proven pattern — every fenced TOML block loads through the real loader and fails on a *silently ignored* key; every printed CLI command names a registered subcommand — applied **only** to a private security runbook | INSTALL-GUIDE / USER-GUIDE / EARLY-ADOPTER-GUIDE carry dozens of copy-pasteable commands and TOML blocks that are the only on-ramp. A relocated settings key or renamed subcommand breaks first-run with nothing failing. The off-loopback guard exists because this class shipped **twice** | **No** | **P1** |
| Two of the largest doc-drift guards are inert in every public checkout | `docs/security/` is **withheld, not missing** — gitignored post-cutover (`.gitignore:144`), ~32 files that exist for the owner. Because a public checkout cannot see them, `tests/test_off_loopback_runbook.py` skips at **module level** (:47-55, 12 tests) and `tests/test_threat_model_doc_drift.py` skips at the **accessor** (:256), so its code-only tests survive but ~88 doc-anchored assertions do not (24 test functions, most doc-anchored) | A silent skip reads as green: the suite reports success and the coverage claim survives. This is the self-defending false-green class `tests/test_cutover_slug_rot.py` was written about | Partially — the accessor-level skip in the threat-model module is a deliberate, documented mitigation | **P1** |
| `verify` still probes the **retired** PySide6 console and names a non-existent extra | `check_console_importable` ([`verify/checks.py:180-195`](../../../messagefoundry/verify/checks.py)) tests `PySide6` importability, labels it "Console importable", and instructs "install the `[console]` extra". `pyproject.toml` declares no `console` extra (`dev, dicom, fhir, harness, otel, postgres, sftp, sqlserver, vault, webauthn, x12, xml`). `docs/testing/VERIFY.md:40` documents the row | The shipped on-box acceptance tool asserts a retired surface and prints an install instruction that cannot work. It escapes `test_install_instruction_provenance.py` because `_EXTRA_REF` matches only `messagefoundry[...]` and `_BARE_NAME_INSTALL` requires a `pip install` verb. Worse: **`verify` has no web-console seam check at all** — the most upgrade-fragile pair on the box is unverified | **No** | **P1** |
| No adopter-facing upgrade smoke over the pinned-wheel path | `windows-service-smoke` installs from the source tree. No leg installs release N-1 from an index, runs traffic, then upgrades in place to N against the same store | This is exactly the sequence EARLY-ADOPTER-GUIDE §13 prescribes and the only one that exercises `_migrate` against a store written by a real prior release under a real service account | **No** | **P1** |
| No rollback rehearsal | EARLY-ADOPTER-GUIDE §13 prescribes config rollback via audited `POST /config/reload` as the primary lever, engine pin-back as secondary, with a warning that store-level changes are not trivially reversible. None of it is exercised as a sequence | Rollback is the 2 a.m. lever. If the reload-root confinement, the quiesce-and-swap, or the dead-letter replay misbehaves under a partially-drained queue, the recovery path itself loses messages | **No** | **P1** |
| No vendor/partner interoperability rig | No Epic / Oracle Health / lab / radiology / HIE fixture; no non-conformant dialect corpus; no live SMART sandbox leg; no real PACS. FHIR `test_connection` does GET `/metadata` but as **reachability only** (`transports/fhir.py:505-508`, `:992`) — the CapabilityStatement body is never parsed and `fhir_version` (`R4B`/`R5`/`STU3`) is never cross-checked against it | Every partner-facing claim — SMART Backend Services against Epic/Oracle Health (`transports/smart.py:5`, FEATURE-MAP:134), DICOM SCU "Mirth-sender parity" (`transports/dicom.py:38`), FHIR version support — rests on loopback mocks. A vendor quirk breaks a live feed with no failing test, surfacing as an `ERROR` disposition on real clinical traffic | **No** | **P1** |
| No end-to-end parallel-run rehearsal | Every piece is built and unit-tested; nothing composes them into one runnable rehearsal. Their authoritative design doc, `TEST-ENVIRONMENT-PLAN.md §5`, is cited from **four** in-repo modules (`harness/reconcile/__init__.py:8`, `__main__.py:3`, `capture.py:3`, `compare.py:4`) and **no such path resolves** — and it is not under one of the withheld directories (`docs/security/`, `docs/reviews/`, `docs/marketing/`), so this is a genuinely dangling citation, not a publishing boundary | The cutover is the highest-consequence operation an adopter performs and its validation tool is proven only component-by-component. A composition defect — capture-sink ACK mode diverging from the real downstream, or `simulate` finalizing in a way that breaks correlation — first appears during a live shadow run | **No** | **P1** |
| Corepoint importer has no regression guard at real-export shape or scale | The only fixtures are 120 lines of hand-authored XML against a real export of 19,682 elements / 187 `<ActionList>` / 14,049 statement positions (`SPEC-105 §0`) | `HANDOFF-105` records the old fixture passing while **0 of 5,013 paths resolved** against real data, because the fixture omitted the connective and the handle: green tests, inert feature. The accounting invariant is still asserted only over 120 synthetic lines | **No** | **P1** |
| Reconcile CLI exit-code gating untested | `_run_compare` returns `0 if result.clean else 1` — the thing a per-connection cutover sign-off gates on. No test drives `python -m harness.reconcile` | If argument parsing silently drops `--blank`/`--sort-segment`/`--ignore-segment` or the exit code stops gating, a sign-off passes on a genuinely divergent feed and the **cutover decision itself is wrong** | **No** | **P1** |
| Scaffolded adopter CI workflow never executed | `tests/test_scaffold_requirements.py` parses `check.yml` as YAML and greps strings; nothing runs `init` → clean venv → `pip install -r requirements.txt` → `messagefoundry check` as a fresh adopter would | A template slip, a renamed flag, or a changed exit code breaks every adopter's CI simultaneously and nothing here fails. `test_scaffold.py` runs `check` **in-process**, which does not exercise the pinned-wheel install path | **No** | **P1** |
| No DST-transition test for the per-connection `Schedule` | `tests/test_connection_scheduler.py:111` sets `America/New_York` but asserts only a plain summer offset. `ActiveWindow.contains` ([`config/models.py:375`](../../../messagefoundry/config/models.py)) is never exercised across spring-forward (the skipped 02:00-03:00 hour) or fall-back (the repeated hour) | A Connection that fails to auto-start on the DST Sunday is an intake outage; one that fails to park stays open outside its contracted window. Both are silent — the scheduler logs a clean start/stop either way and no disposition changes | **No** | **P2** |
| Retention VACUUM and DR-backup schedules use bare system-local wall clock | `retention.py:997-1010` and `dr_backup.py:241-243`, `:960` use `time.localtime` with a local-day dedupe key (`_day_key`) and **no operator-visible IANA zone** — unlike `Schedule`, which is zone-declared | A missed nightly backup or purge on the DST Sunday is invisible until a restore or a HIPAA retention audit; a host timezone change silently moves the maintenance window with no config change | **No** | **P2** |
| No locale / non-UTF-8 codepage coverage beyond `--help` | `locale` is unused across `messagefoundry/`; hardening is a `reconfigure(errors="replace")` at `__main__.py:49-56` plus `_safe_print` (`:4283`). One test exists: `tests/test_cli.py:43`, top-level `--help` only | On a stock non-en-US Windows Server the console codepage is not cp1252. A config path, partner name, or log line with non-ASCII can raise `UnicodeEncodeError` inside CLI output or NSSM-captured stdout — and the guides' copy-paste commands are the first thing to hit it | Partially (one surface) | **P2** |
| `FEATURE-MAP.md` is materially stale in this area | §10 "Surfaces — Admin Console (PySide6)" (:162-173) lists ✅ rows for a **retired** console; §13 (:209-211) marks version single-sourcing, CHANGELOG refresh, and `release.yml` as 🔨 though all three shipped; the whole MIG surface (Corepoint import, tee, reconcile, timezone, verify, support bundle, update-check) has **no row at all** | It is the public status catalog an evaluator reads. `test_feature_map_claims.py` guards only ASVS-score quoting and link targets; nothing asserts a row matches reality | **No** | **P2** |
| An adopter guide denies a shipped route | `docs/EARLY-ADOPTER-GUIDE.md:635` states "there is no Prometheus exporter" while `api/app.py:4146` registers `GET /metrics` (and `:4156` `/metrics/history`), shipped per FEATURE-MAP:159 | An adopter builds day-2 monitoring on log-scraping — a worse posture than what ships, and one that makes the EARLY-ADOPTER-GUIDE §11 Stage 2 "monitoring in place" gate harder to meet than necessary | **No** | **P2** |
| `messagefoundry-harness` publish is variable-gated while a test asserts it published | `release.yml:20-24` gates the harness PyPI publish behind repo variable `PUBLISH_HARNESS`; `tests/test_install_instruction_provenance.py:56` lists it in `_PUBLISHED_DISTRIBUTIONS` | An adopter following the guide to install the harness — the tool the EARLY-ADOPTER-GUIDE §11 Stage 0 gates depend on — may hit an unresolvable install | **No** | **P2** |
| IDE schema-version constant is duplicated and cross-language-unpinned | `SUPPORTED_SCHEMA_VERSION = 1` appears in **both** `ide/src/connectionForm.ts:74` and `ide/src/connectionSchemaModel.ts:21`; neither is pinned against the engine's `SCHEMA_VERSION` (`config/connection_schema.py:45`) | The forward-compat *refusal* is correct and tested — the residual is two constants that can diverge from each other, silently splitting `isSupported()` from `parseConnectionSchema()` | Partially — the refusal is tested; the duplication is not | **P2** |
| `check`'s dryrun green-skips a present-but-empty fixtures dir | `_check_dryrun` returns `ok=True, skipped=True` when a real dir contains no `**/*.hl7` ([`checks.py:1061-1066`](../../../messagefoundry/checks.py)). An explicitly-given *missing* path correctly FAILS | The adopter's merge gate reads green while asserting nothing about behaviour. The scaffold *does* ship `messages/sets/example_adt.hl7`, so a fresh repo is fine — the hazard is an adopter who empties or relocates the corpus | Documented at ADOPTER-CI §5.1; not surfaced at runtime as a distinct verdict | **P2** |
| No day-2 support-workflow validation | `support bundle` content is well tested; nothing exercises the loop *reproduce a fault → collect → the bundle diagnoses it* | SUPPORT-POLICY sets a vendor fast-response SLA that depends on the adopter's first artifact being sufficient. A bundle that redacts away the diagnostic signal, or omits the failing Connection, turns a 1-day triage into a multi-round PHI-risky exchange | **No** | **P2** |

### 17.4 Test matrix

**Row class (`Cls`).** **T** = *Test* — a falsifiable assertion with an observable pass criterion; **only T rows count toward the release gate**. **C** = *Characterisation* — produces a recorded measurement, finding or dated owner decision with no threshold yet; legitimate work, but it **cannot fail**, so it never gates a release, and it becomes a T row the day its threshold or decision is recorded. **A** = *Assurance* — an external engagement (penetration test, third-party review, DAST); blocking only for an off-loopback / production-exposure release, advisory otherwise, and excluded from the ordinary P0 count.

**This chapter has 78 rows: 71 T, 7 C, 0 A.** Among the 71 T rows, **13 are P0** (MIG-01, MIG-02, MIG-06, MIG-07, MIG-08, MIG-11, MIG-14, MIG-15, MIG-36, MIG-37, MIG-38, MIG-45, MIG-75). The 7 C rows are MIG-33 (coverage owned in another document), MIG-56 (a third-party sandbox that may legitimately SKIP), MIG-60 and MIG-64 and MIG-73 (dated owner decisions / filled checklists), and MIG-70 and MIG-72 (rehearsals whose output is a written finding). No row in this chapter is an external assurance engagement, so **A = 0**; the plan's A rows live in the SEC chapter.

**Pointer rows** carry Method `—`, Cls `T` and no separate work: MIG-18 → TRAY, MIG-35 → MIG-74, MIG-44 → W25:S3.10, MIG-48 → PUB-49. MIG-33 is not a pointer but a **C** row: its falsifiable assertion is owned by another document (FCP:CLI-17), so it records a closure rather than gating this release.

**Two consolidated deliverables are owned here**, each collapsing rows that were duplicated across the plan into one specified row:

- **MIG-28 — the FEATURE-MAP drift guard.** PIPE-36, PIPE-37, STORE-53, HA-57, CFG-55, PUB-64, API-66, API-67, WEB-55, IDE-67, ALERT-65, SEC-08, PERF-56, CONN-32 and CONN-33 point at MIG-28; no separate work is scoped in those chapters.
- **MIG-74 — the "doc paths resolve" linter.** WEB-56 and SEC-09 point at MIG-74, and MIG-35 (the BACKLOG-reference classifier) folds into it, so the publishing boundary is declared in exactly one allowlist.
- **MIG-06 / MIG-09 / MIG-10 — the store vintage / schema-upgrade matrix**, absorbed from STORE-40, STORE-41, STORE-42 and STORE-43. MIG-06 owns the **pre-migration fixture DB** (a per-vintage DDL generator — never a committed binary — so the DB the current engine opens has genuinely never seen `_migrate`); MIG-09 owns the **ADR 0064 alternating-hash opens** (current → prior → current → prior against one persistent store, four opens, marker must not thrash); MIG-10 owns re-run idempotence across that cycle.

| ID | Test | Type | Method | Env | Backend | Cls | Pri | Pass criteria |
|---|---|---|---|---|---|---|---|---|
| MIG-01 | Backend switch with a **populated** SQLite store is refused or loudly warned | Negative/Security | pytest | dev-PC | x3 | T | P0 | Open a SQLite store, ingest N=25 synthetic messages leaving `in_pipeline > 0`, then re-open with `StoreSettings.backend` set to a server backend against the same config root. A WARNING-or-above log record is emitted naming (a) the abandoned SQLite path and (b) its non-zero `in_pipeline_depth()`; under the fail-closed posture flag the open raises. Assert the record exists, is not DEBUG/INFO, and carries both facts. Test fails today |
| MIG-02 | Backend switch with a **drained** SQLite store starts clean | Functional | pytest | dev-PC | x2 | T | P0 | Same fixture with `in_pipeline_depth() == 0`: the server-DB open succeeds and emits at most one INFO-level "prior SQLite store detected, drained" record — no WARNING, no refusal |
| MIG-03 | The greenfield-only claim is single-sourced across all six documents | Compat | pytest | dev-PC | n/a | T | P1 | A doc-drift test asserts the greenfield-only sentence in `DEPLOY-SERVER-DB.md:11`, `EARLY-ADOPTER-GUIDE.md:100`, `FEATURE-MAP.md:98`, `SYSTEM-REQUIREMENTS.md:113`, `AOAG-DEPLOYMENT.md:309`, `releases/v0.1-PLAN.md:110` all resolve, and that whichever code path implements MIG-01 is named by at least one of them |
| MIG-04 | `in_pipeline` — the cutover checklist's drain signal — is a real, reachable field | Functional | pytest | dev-PC | x3 | T | P1 | `GET /status` (`api/app.py:4134`) and `GET /metrics` both expose `in_pipeline` sourced from `store.in_pipeline_depth()`; the value is 0 on a fresh store, N after N un-drained sends, back to 0 after drain. `DEPLOY-SERVER-DB.md:274`'s checkbox names a field an operator can actually read |
| MIG-05 | No shipped text advertises a cross-backend migration tool | Compat | pytest | dev-PC | n/a | T | P2 | No `docs/*.md` or CLI help string offers a SQLite→server-DB copy/convert command. BACKLOG #180 (:5940) remains the only place it is described, and is marked planned |
| MIG-06 | SQLite vintage matrix — forward open (N-3 … N-1 → current) from a **pre-migration fixture DB** | Upgrade | pytest | dev-PC | SQLite | T | P0 | **Owner of the store vintage / schema-upgrade matrix** — STORE-40, STORE-41, STORE-42 and STORE-43 point at MIG-06/MIG-09/MIG-10; no separate work is scoped there. The fixture is a **pre-migration DB**: a checked-in generator per vintage V emits the *exact DDL as it stood at vintage V* — before the current `_MIGRATION_REV` and before the ADR 0060 seq-trailing index rename — plus a handful of rows, so the DB the current engine opens has genuinely never seen `_migrate`. No binary `.db` is committed; a generator keeps the fixture reviewable and PHI-free by construction. For each vintage, open with the current engine and assert: `_migrate` ([`store/store.py:2933`](../../../messagefoundry/store/store.py)) completes; every column in `_MESSAGE_MIGRATIONS`, plus `audit_log.row_hash`, `audit_log.client`, `users.channel_scope`, `roles.permissions`, is present; every pre-existing message row is readable with its original `status` and decrypts under the configured key; `in_pipeline_depth()` matches the vintage's queued count; and the post-open `schema_meta` hash equals the current `_schema_hash()` |
| MIG-07 | Server-backend vintage matrix — forward open | Upgrade | pytest + CI-leg | container-CI | x2 | T | P0 | Same assertions against SQL Server and PostgreSQL **pre-migration** vintage schemas, driven off `_schema_hash()` / `_MIGRATION_REV`. A vintage marker forces a full guarded re-run (ADR 0064); the applock precedes every CREATE (extends `test_sqlserver_schema_init.py:175`) |
| MIG-08 | **Downgrade** — a store written by the current engine, opened by the prior released wheel | Upgrade | CI-leg | container-CI | x3 | T | P0 | Install the previous published engine wheel into a clean venv; open the current-engine store. Either it opens and every prior message row is readable, **or** it refuses with a message naming the schema mismatch. A silent partial open, a dropped column, or a re-stamped older `schema_meta` hash is a FAIL — those three outcomes are what makes this row falsifiable rather than a recorded observation. The surviving branch is written up as the authoritative answer to §17.9 Q6 |
| MIG-09 | ADR 0064 **alternating-hash opens** are pinned | Upgrade | pytest | container-CI | x2 | T | P1 | Beyond a single newer-hash open: drive the **alternating** sequence current → prior → current → prior against one persistent store, four opens, asserting after each. With a `schema_meta` row carrying a *newer* hash than the running engine's `_schema_hash()`, the documented behaviour holds (full guarded re-run of the older DDL batch, or refusal) and the older DDL batch cannot drop, rename, or narrow a column that only the newer schema defines. Across the alternation the hash marker must not thrash — the newer engine must not have to re-run its full batch on every re-open after an older engine re-stamped the marker — and no column added by the newer schema is lost at any point in the cycle |
| MIG-10 | Migration re-run idempotence across all vintages, including on re-open after an alternating-hash cycle | Upgrade | pytest | dev-PC | x3 | T | P1 | Opening each migrated vintage a second time performs zero DDL (probe count / marker skip) and leaves the schema byte-identical. Repeat the second open **after** MIG-09's alternating cycle on the same store: idempotence must survive an older engine having touched the marker in between |
| MIG-11 | Encrypted bodies survive the vintage migration | Upgrade | pytest | dev-PC | x3 | T | P0 | A vintage store written with an active key opens under the current engine and every `raw`/`payload` decrypts; a vintage written *plaintext* is encrypted on migration (extends `test_staged_pipeline.py:797`) and no plaintext body remains at rest |
| MIG-12 | Crash mid-migration re-runs cleanly | HA/Resilience | pytest | dev-PC | x3 | T | P1 | Interrupt the open between two ALTERs (monkeypatched raise); the next open completes the migration and reaches the same final schema. No row is lost; `in_pipeline_depth()` is unchanged |
| MIG-13 | Legacy `outbox` → `queue` remains covered (regression pin only) | Upgrade | pytest | dev-PC | SQLite | T | P2 | Cite `tests/test_staged_pipeline.py:797` and `:989`. Add only an assertion that the migrated rows land at `stage='outbound'` with a non-null `destination_name` and that orphans are logged, not dropped |
| MIG-14 | A seam-mismatched engine/console pair fails loud with an actionable message **and the blast radius is documented** | Upgrade | pytest | dev-PC | n/a | T | P0 | With `[api].serve_ui = true` (the default) and a console declaring a neighbouring seam, `create_app` raises `UiSeamMismatch`; the message names both seam integers and the fix. Additionally assert that **`serve` does not start** — i.e. the failure is engine-wide, not `/ui`-only — so the runbook text in MIG-16 is written against the true consequence |
| MIG-15 | The web console declares a PEP 508 engine range | Upgrade | pytest | dev-PC | n/a | T | P0 | `packaging/messagefoundry-webconsole/pyproject.toml` `[project].dependencies` contains a bounded `messagefoundry>=X,<Y` specifier, not a bare name; and `WEBCONSOLE-PACKAGE.md` §2's install-time layer-1 claim is asserted against that specifier. Fails today (bare `"messagefoundry"`, :42) |
| MIG-16 | The upgrade runbooks name every wheel that must move together | Compat | pytest | dev-PC | n/a | T | P1 | Drive from the packaging tree, not a hardcoded list: for every distribution this repo builds that an operator installs alongside the engine, the upgrade sections of `INSTALL-GUIDE.md` §10 and `EARLY-ADOPTER-GUIDE.md` §13 name it whenever `[api].serve_ui` is in scope. Fails today — both bump only `messagefoundry==<new>` |
| MIG-17 | `verify` reports the engine↔console seam | Functional | pytest | dev-PC | n/a | T | P1 | A new `host.webconsole` check reports PASS with both seam values when the installed console supports `ENGINE_UI_SEAM`, FAIL naming both values when it does not, and SKIP when `serve_ui` is off or the console is absent. Report renders in `--report-md` and `--report-json` |
| MIG-18 | `verify` stops asserting the retired PySide6 console and stops naming a non-existent extra → **pointer** | Negative/Security | — | dev-PC | n/a | T | P1 | **Covered by TRAY; no separate work scoped.** TRAY owns the `[console]` extra / `check_console_importable` provenance deliverable (WEB-58 also points there). Context retained for this chapter's readers only: `check_console_importable` ([`verify/checks.py:178-192`](../../../messagefoundry/verify/checks.py)) still probes `PySide6` as "the console" and its SKIP message at :185 instructs "install the `[console]` extra", which `pyproject.toml` does not declare; `docs/testing/VERIFY.md:40` documents the row. The widened extra-detector in `tests/test_install_instruction_provenance.py` (matching a bare `[<name>] extra` phrase, not only `messagefoundry[...]` — the pattern that let this through) lands with TRAY's row |
| MIG-19 | IDE `SUPPORTED_SCHEMA_VERSION` is single-sourced and pinned to the engine | Compat | ide-mocha + pytest | dev-PC | n/a | T | P2 | The TS constant is defined once (currently duplicated at `connectionForm.ts:74` and `connectionSchemaModel.ts:21`) and a pytest assertion pins it against `config/connection_schema.SCHEMA_VERSION`, so a `SCHEMA_VERSION` bump without an extension bump reds the engine suite. The existing loud-refusal test (`connection-schema.test.ts:79`) stays |
| MIG-20 | Engine and harness stay lockstep at build time | Compat | pytest | dev-PC | n/a | T | P2 | `packaging/messagefoundry-harness/pyproject.toml` sources its version from `messagefoundry/__init__.py` (:13) and depends on `messagefoundry[harness]` (:41); assert both, and that no doc instructs a harness version different from the engine's |
| MIG-21 | Every `messagefoundry==X.Y.Z` in shipped text equals `__version__` | Compat | pytest | dev-PC | n/a | T | P1 | Extract every literal engine pin from `docs/*.md` + `README.md` + `messagefoundry/**/*.py` and assert equality with `messagefoundry.__version__`. Fails today: 0.1.0 in 4 documents vs 0.3.2. Placeholders (`<version>`, `X.Y.Z`, `$V`) are explicitly exempt and the exemption list cannot rot vacuous |
| MIG-22 | Every `messagefoundry-webconsole==X.Y.Z` equals the console `__version__` | Compat | pytest | dev-PC | n/a | T | P1 | Same extractor over the console distribution. `INSTALL-GUIDE.md:252` (`==0.2.15`) and `USER-GUIDE.md:113` must equal `messagefoundry_webconsole.__version__` |
| MIG-23 | The install guide's description of the scaffolded `requirements.txt` matches what `init` writes | Compat | pytest | dev-PC | n/a | T | P1 | Scaffold into a tmp dir and assert `INSTALL-GUIDE.md:144`'s quoted `requirements.txt` line equals the generated file's content. Fails today (`0.1.0` quoted, `__version__` generated) |
| MIG-24 | Every fenced `toml` block in the adopter guides loads through the real settings loader | Compat | pytest | dev-PC | n/a | T | P1 | Port the `test_off_loopback_runbook.py:179` pattern to `INSTALL-GUIDE.md`, `USER-GUIDE.md`, `EARLY-ADOPTER-GUIDE.md`, `ADOPTER-CI.md`, `DEPLOY-SERVER-DB.md`, `CONFIGURATION.md`: each block loads via `load_settings` **and** raises on a silently-ignored key (an unrecognized key only warns, so "it loads" alone is not enough) |
| MIG-25 | Every `# connections.toml` block in the adopter guides loads through the strict loader | Compat | pytest | dev-PC | n/a | T | P1 | Port `test_off_loopback_runbook.py:218`: each `[[inbound]]`/`[[outbound]]` block loads through the unknown-key-rejecting `connections.toml` loader and desugars into `Registry` entries |
| MIG-26 | Every `messagefoundry <sub>` printed in the adopter guides is a registered subcommand | Compat | pytest | dev-PC | n/a | T | P1 | Port `test_off_loopback_runbook.py:89`: pin every invocation against the argparse dispatch table in `messagefoundry/__main__.py` (`_import`, `init`, `check`, `verify`, `serve`, `generate`, `connection`, `service`, …). Also assert every named flag exists on that subparser |
| MIG-27 | No adopter guide denies the existence of a registered API route | Compat | pytest | dev-PC | n/a | T | P2 | Drive from the FastAPI route table, not a hardcoded list: no guide contains a "there is no `<route>`" / "no `<X>` exporter" claim for a path the app registers. Fails today — `EARLY-ADOPTER-GUIDE.md:635` vs `/metrics` at `api/app.py:4146` |
| MIG-28 | **FEATURE-MAP drift guard** — every ✅/🔨 mark resolves against code, and every ✅ row has an owning test | Compat | pytest | dev-PC | n/a | T | P1 | **Owner of the plan-wide FEATURE-MAP drift guard** — PIPE-36, PIPE-37, STORE-53, HA-57, CFG-55, PUB-64, API-66, API-67, WEB-55, IDE-67, ALERT-65, SEC-08, PERF-56, CONN-32 and CONN-33 point here; no separate work is scoped in those chapters. **One** extension of the existing `tests/test_feature_map_claims.py` (which today guards only ASVS-score quoting and link targets), asserting three properties over every row of `docs/FEATURE-MAP.md`: **(a) path truth** — no ✅ row names a module path that does not exist on disk (kills §10's retired PySide6 console rows, :162-173); **(b) test ownership** — every ✅ row resolves to at least one owning test (a named test module/node id that exists and contains a matching test), so a ✅ can never rest on prose alone, and the owner map cannot rot vacuous (an empty or all-exempt map FAILS); **(c) status truth** — no 🔨 row names an artifact that exists and is exercised by a green CI leg (kills §13's `release.yml` / version-single-sourcing / CHANGELOG rows, :209-211), and every top-level CLI subcommand in `messagefoundry/__main__.py`'s dispatch table has a row. Withheld evidence is handled per §17.1: a row citing a `docs/security/**` document is classified `WITHHELD`, never a broken path |
| MIG-29 | A doc-anchored suite may not silently skip where the docs are present | Negative/Security | pytest | container-CI | n/a | T | P1 | A meta-test: when `MEFOR_PRIVATE_DOCS=1` is declared — i.e. the runner has the **withheld** `docs/security/` set materialized — any module that would `pytest.skip` for an unreadable `docs/security/**` file instead **fails**. Combined with a scheduled private-docs CI leg, this turns the ~100 assertions that are inert in a public checkout into either green or red — never silently absent. Skipping in a *public* checkout stays correct (the document is withheld, so the guard has nothing to read); what must not survive is a skip on a runner that *does* have them |
| MIG-30 | The skip census is published and cannot grow silently | Compat | pytest | container-CI | n/a | T | P2 | Run the suite with `-rs` and assert the set of module-level skips equals a checked-in allowlist with a stated reason each. A new silent skip reds the gate |
| MIG-31 | Corepoint importer pinned against a synthesized **real-grammar** corpus | Functional | pytest | dev-PC | n/a | T | P1 | Generate a large synthetic `<Package>` from the SPEC-105 grammar — role-tagged `@Data` spans (`keyword`/`path`/`literal`/`input-handle`/`other-handle`/`variable`/`detail`/`description`), dash coordinates (`PID-5-1`), `/`-leading path spans, nested `<If>`/`<Try>`/`<Foreach>`/`<Call>`, branch-group wrappers with no `@Data`, and `@Disabled` at statement/`<Block>`/`<ActionList>`/`<Package>` level. Pin the mapped/unmapped/disabled ledger; pin the `if False:` and bare-`else:` counts; assert every emitted module compiles (`ast.parse`) and passes `messagefoundry check`. **No customer export is committed, quoted, or pasted** |
| MIG-32 | The import `--json` ledger exposes its own denominator | Functional | pytest | dev-PC | n/a | T | P1 | `ImportResult.to_json()` (`corepoint_import.py:252-288`) emits a `total_statements` alongside mapped/unmapped/disabled, and `mapped + unmapped + disabled == total_statements` for every fixture. Today the CLI reports three numerators and no denominator, so a migrator cannot see the accounting invariant the tests assert internally |
| MIG-33 | `import corepoint` CLI wrapper — end-to-end | Functional | pytest | dev-PC | n/a | C | P2 | **Owned as FCP:CLI-17** (FEATURE-COVERAGE-PLAN :185, :478, :1442). Close it there: drive `main(["import","corepoint",...])` for text and `--json`, assert exit codes and the summary shape. Two cases already exist (`test_corepoint_import.py:945`, `:968`) — the row is the argparse-surface and flag coverage, not a rewrite. **C**, because the falsifiable assertion lives in another document's ID space: this row records that FCP:CLI-17 moved off `partial`, so it cannot fail here and must not gate a MessageFoundry release. It becomes **T** the day the argparse-surface coverage is scoped into this plan instead |
| MIG-34 | A `SPEC-105` specified-but-unbuilt mapping is reported as unmapped **with its cause**, never claimed | Functional | pytest | dev-PC | n/a | T | P2 | `EnvLogText` has a built vocabulary target (`messagefoundry/diagnostics.py:36` `log_note`) but no importer mapping (`grep EnvLogText corepoint_import.py` → none). Assert an `EnvLogText` statement lands in the unmapped bucket with a named `_decline_reason`, and that the emitted `# TODO: Corepoint …` marker names `log_note` as the hand-finish target. The same shape for the three-part `MsgSend` fix |
| MIG-35 | Every BACKLOG cross-reference resolves or is classified above the published baseline → **pointer** | Compat | — | dev-PC | n/a | T | P1 | **Covered by MIG-74; no separate work scoped.** The BACKLOG-reference classifier folds into MIG-74's doc-path linter so the publishing boundary is declared in exactly one allowlist rather than a second time inside `scripts/docs/backlog_status_check.py`. Context retained for this chapter's readers: a `#NNN` reference must resolve to a `## NNN.` section **or** be classified above-baseline — the published index ends at `## 231.` (`docs/BACKLOG.md:6883`, boundary prose at `:6041`) — and an above-baseline number such as #313 (cited at `:269` and `:4394`) is **valid evidence**, so the classifier must assert `NNN > 231` rather than tolerating any unresolved reference |
| MIG-36 | `reconcile compare` default output is PHI-safe | PHI | pytest | dev-PC | n/a | T | P0 | Build a needle-bearing synthetic corpus (unique tokens in PID-5, PID-3, PID-7, OBX-5) with a deliberate mismatch. With no diff flag, assert **no needle** appears in stdout, stderr, or the `--report-json` artifact; only counts and match keys. Fails today: `report.py` `render_text` prints field values at :63-66 and `render_json` always emits `left`/`right` at :31-42 |
| MIG-37 | `reconcile compare` gates diff disclosure behind an explicit, warned flag | PHI | pytest | dev-PC | n/a | T | P0 | A `--show-diffs` flag (mirroring `tee/__main__.py:269`) is required to reveal field values, and using it prints a PHI/TEST-DATA-ONLY warning to stderr before any diff (mirroring `tee/__main__.py:451-453`). Without it the exit code and counts are unchanged |
| MIG-38 | The reconcile capture CLI makes de-identification the reachable path | PHI | pytest | dev-PC | n/a | T | P0 | The `capture` subparser exposes `--anonymize --salt-env <VAR>` wiring `CaptureSink(anonymizer=…)` (the kwarg already exists at `capture.py:46`); with it set, no un-anonymized body reaches the JSONL, verified with the ADR 0030 fail-closed leak gate. Without it, a stderr banner states that bodies are written verbatim. Fails today — `_run_capture` never passes the kwarg and no flag exists |
| MIG-39 | `reconcile compare` exit code gates a per-connection sign-off | Functional | pytest | dev-PC | n/a | T | P1 | `python -m harness.reconcile compare` exits **0** on a matching synthetic golden pair and **non-zero** on a single real field difference; the `--report-json` artifact has the documented shape (`connection`, `clean`, `counts.*`, `mismatches`, `mefor_only`, `corepoint_only`, `duplicate_keys`) |
| MIG-40 | Each normalize flag demonstrably changes the verdict | Functional | pytest | dev-PC | n/a | T | P1 | Craft a pair that is dirty by default and clean under exactly one of `--blank SEG-N`, `--sort-repetition SEG-N`, `--sort-segment SEG`, `--ignore-segment SEG`, and assert the flip for each. A flag that never changes a verdict is a silently-dropped argument |
| MIG-41 | End-to-end parallel-run rehearsal composes | Functional | harness | dev-PC | SQLite | T | P1 | A scripted rehearsal: synthetic MLLP sender → `tee` relay fan-out → two engines (one with `[shadow].simulate_all_egress`) → `reconcile capture` sinks → `reconcile compare` exits **0**. Then swap in a deliberately divergent Handler and assert non-zero. Assert the capture sink's ACK mode matches the shadow outbound's configured `AckMode`, and that `simulate` outbounds still reach a terminal disposition that correlation can key on |
| MIG-42 | The reconcile harness's design doc resolves | Compat | pytest | dev-PC | n/a | T | P2 | Four modules cite `TEST-ENVIRONMENT-PLAN.md §5` (`harness/reconcile/__init__.py:8`, `__main__.py:3`, `capture.py:3`, `compare.py:4`) and no such path resolves. This one is **not** behind the publishing boundary — it is not under `docs/security/`, `docs/reviews/` or `docs/marketing/` — so the citation dangles for every reader, owner included. Publish a synthetic-only in-repo version **or** re-point the four citations; the mechanical check that holds it closed afterwards is MIG-74's linter, not a second guard here |
| MIG-43 | `tee compare` default output carries no field values | PHI | pytest | dev-PC | n/a | T | P1 | Complements MIG-36 on the sibling tool. `tee/report.py` is already `include_diffs`-gated and `tee/__main__.py:451` already warns — add the capsys/caplog needle assertion so the default path is pinned, and assert `--capture-bodies` (`relay.py:66`) defaults off. Cite **FCP:ANON-9** (FEATURE-COVERAGE-PLAN :1359) for `anonymize-captures`; do not duplicate it |
| MIG-44 | On-box reconcile capability proof under the service identity → **pointer** | Functional | — | W2025-box | SQLite | T | P2 | **Covered by W25:S3.10** (WIN2025-TEST-PLAN :622-641); no separate work scoped. This chapter's only contribution is an ordering constraint: MIG-36/37/38 must be merged **before** W25:S3.10 runs, so the on-box proof cannot produce a PHI-bearing artifact |
| MIG-45 | Config rollback under load loses nothing | HA/Resilience | pytest | dev-PC | x3 | T | P0 | Promote a config whose Handler raises, under a sustained synthetic send. Roll back via audited `POST /config/reload` to the last-known-good dir (inside an allow-listed reload root). Assert: every ingress row reaches a terminal disposition; the count of `RECEIVED` + `ROUTED` rows still in flight goes to zero; no row is lost between the quiesce and the swap; the reload is audited with the acting user |
| MIG-46 | Dead-letter replay recovers messages stranded by the bad transform | HA/Resilience | pytest | dev-PC | x3 | T | P1 | After MIG-45's rollback, replay the dead-letters the bad Handler produced and assert each reaches `PROCESSED` through the good Handler, with the **original raw** preserved and the delivery audited |
| MIG-47 | Engine pin-back rollback rehearsal | Upgrade | CI-leg | container-CI | x3 | T | P1 | Install release N, ingest synthetic traffic, stop; install release N-1; start. Record the observed outcome against MIG-08's finding. PASS is *either* a clean start with all prior dispositions readable *or* a loud refusal — never a silent partial open. The result is written into EARLY-ADOPTER-GUIDE §13 as the supported answer |
| MIG-48 | Reload-root confinement holds during rollback → **pointer** | Negative/Security | — | dev-PC | SQLite | T | P1 | **Covered by PUB-49; no separate work scoped.** PUB-49 owns reload-root confinement. Context retained for this chapter: S-MIG-F step 7 exercises the same refusal **mid-drain** — a `POST /config/reload` naming a directory outside the allow-listed reload roots must be refused with an audited denial and no partial swap, with rows still in flight. That mid-drain case rides MIG-45's module (`tests/test_config_rollback.py`), not a second confinement row here |
| MIG-49 | N-1 → N in-place upgrade smoke on Windows against one store | Upgrade | CI-leg | W2025-box | SQLite | T | P1 | Install the previous published wheel from an index (or a local wheelhouse), `serve` under NSSM, ingest N=50 synthetic messages, stop; install the current wheel; restart. Assert `/health` is ok, the prior messages' dispositions are intact and their raw bodies readable, and new traffic flows end to end. This is the only leg that exercises `_migrate` against a store written by a real prior release under a real service identity |
| MIG-50 | The scaffolded adopter workflow is executed for real | Functional | CI-leg | container-CI | SQLite | T | P1 | A job that runs `messagefoundry init` into a temp dir, creates a clean venv, installs the engine from the locally built wheel, then runs the scaffolded workflow's **exact** commands: `messagefoundry check --config config --messages messages/sets --no-lint` (exit 0) and `pip-audit -r requirements.txt --desc`. Also assert `pip download -r requirements.txt --no-deps --only-binary=:all:` resolves the pinned version |
| MIG-51 | The adopter gate reds on a broken graph | Negative/Security | CI-leg | container-CI | SQLite | T | P1 | In the scaffolded repo, break the inbound's `router` binding and assert `messagefoundry check` exits non-zero with `validate` FAIL; then restore it, break the Handler so a fixture errors, and assert `dryrun` FAIL. A green run on either is a FAIL of this row |
| MIG-52 | A fixture-less dryrun renders SKIPPED-with-reason, not silence | Usability | pytest | dev-PC | n/a | T | P2 | `messagefoundry check` against a present-but-`.hl7`-empty messages dir renders an explicit `dryrun: SKIPPED (no *.hl7 fixtures in <path>)` row in both text and `--json`, and the scaffolded workflow emits a GitHub `::warning::` naming it. Cite ADOPTER-CI §5.1 for the rationale; the row is about surfacing, not changing, the semantics |
| MIG-53 | A doc-instructed index install is backed by an ungated publish | Compat | pytest | dev-PC | n/a | T | P2 | For each distribution named in a bare-name install instruction, assert its `release.yml` publish step is not repository-variable-gated, **or** that `_PUBLISHED_DISTRIBUTIONS` records it as claimed with evidence. Fails today for `messagefoundry-harness`: gated on `PUBLISH_HARNESS` (`release.yml:20-24`) yet listed as published (`test_install_instruction_provenance.py:56`) |
| MIG-54 | Offline vendor-dialect corpus survives peek → strict validate → a representative Handler | Functional | pytest | dev-PC | SQLite | T | P1 | A synthetic corpus of documented real-world dialect quirks — Epic-style Z-segments, non-standard MSH-9 triplets, empty-vs-absent components, mixed CRLF/LF/CR segment terminators, non-default encoding characters read from MSH, an oversized OBX-5, a repeating field with a trailing separator, a missing MSH-12. Each must: peek without raising, produce the expected routing decision, and either validate or land in `ERROR` with a named cause. **No real PHI; no partner-identifying names, site codes, or hostnames** |
| MIG-55 | FHIR `test_connection` verifies conformance, not just reachability | Functional | pytest | dev-PC | n/a | T | P1 | Extend the `/metadata` probe (`transports/fhir.py:505-508`, `:992`) to parse the `CapabilityStatement` and compare its `fhirVersion` against the outbound's declared `fhir_version` (`R4B`/`R5`/`STU3`, `config/wiring.py:1378-1380`), reporting a clear mismatch. Today the body is fetched and discarded. Include a negative case: a server declaring STU3 against an R4B-configured Connection fails the probe with a named reason |
| MIG-56 | Opt-in live interoperability leg against public sandboxes | Compat | CI-leg | cloud | n/a | C | P2 | An env-gated `interop` job against a public HAPI FHIR test server (R4B and R5) and the SMART Health IT launcher: token acquisition through `transports/smart.py`, one resource POST, one `fhir_lookup` read, and the MIG-55 conformance probe. **Skips loud** (a reported SKIP with the unreachable endpoint named) when credentials or network are absent — never a silent pass |
| MIG-57 | DICOM peer conformance beyond loopback pynetdicom | Compat | CI-leg | container-CI | n/a | T | P2 | Stand up dcm4chee or Orthanc in a service container with configured AE titles and an explicit accepted-SOP-class list. Assert C-ECHO succeeds (`transports/dicom.py:689-704`), a headers-only C-STORE of a synthetic SR is accepted, a **rejected** SOP class maps to a permanent `NegativeAckError` → dead-letter (not a retry loop), and DICOMweb STOW-RS accepts the same object. No pixel data |
| MIG-58 | X12 partner envelope variants parse and route | Compat | pytest | dev-PC | n/a | T | P2 | Synthetic ISA/IEA envelopes in partner-specific variants (differing element/sub-element/repetition separators, segment terminator with and without a trailing newline, ISA fixed-width padding, a multi-GS interchange) each split correctly through `parsing/x12/` and route via `X12Peek` — exercised as `content_type=x12` `RawMessage`, never HL7-parsed |
| MIG-59 | WSDL import against partner-shaped service descriptions | Compat | pytest | dev-PC | n/a | T | P2 | Extend `tests/test_wsdl_import.py` (ADR 0122) with two partner-shaped WSDLs: one with imported schemas and one with a document/literal-wrapped operation. Assert the pure type tree builds, validation against the WSDL rejects a malformed body, and no `zeep` import appears |
| MIG-60 | Per-partner conformance checklist is filled, not assumed | Compat | manual | any | n/a | C | P2 | For each named partner in an adopter's scope, a filled row: HL7 version + dialect deviations, ACK mode, MSH-9 triplet form, character set, FHIR version + auth mode, DICOM SOP classes + AE titles, and who confirmed each. An unfilled row blocks that partner's Stage 2 gate (EARLY-ADOPTER-GUIDE §11) |
| MIG-61 | `ActiveWindow` behaves across spring-forward and fall-back | Functional | pytest | dev-PC | n/a | T | P1 | Parametrize `ActiveWindow.contains` / `Schedule.is_active` (`config/models.py:375`, `:411`) over the 2026 and 2027 US (`America/New_York`) and EU (`Europe/London`) transition instants, for windows that **straddle**, **start inside**, and **end inside** the skipped 02:00-03:00 hour and the repeated hour, for same-day and past-midnight spans, with `invert` both ways. Every case asserts the intended up/parked state, not merely "does not raise" |
| MIG-62 | The runner actually parks/starts a scheduled Connection across a DST transition | HA/Resilience | pytest | dev-PC | SQLite | T | P1 | Drive the `RegistryRunner` scheduler task with a monkeypatched clock stepping across spring-forward: an inbound Connection whose window opens inside the skipped hour still starts exactly once, and one whose window closes inside the repeated hour parks exactly once (no double park/resume) |
| MIG-63 | Retention VACUUM and DR backup fire exactly once per local day across a DST transition | Functional | pytest | dev-PC | SQLite | T | P2 | Freeze the clock across both transitions in a DST zone (monkeypatch `TZ` + `time.localtime`) and assert `_vacuum_due` (`retention.py:997`) and `_backup_due` (`dr_backup.py:236`) each fire exactly once per local day — including when the configured target time falls inside the skipped hour — and that `_day_key` does not produce a doubled pass on fall-back |
| MIG-64 | The maintenance-window timezone is operator-visible | Usability | pytest | dev-PC | n/a | C | P2 | Either `[retention]` / `[dr]` gain an explicit IANA zone (validated like `ActiveWindow._check_timezone`, `config/models.py:355`), or `CONFIGURATION.md` states unambiguously that both follow the host's system timezone and that changing it moves the window. Assert whichever is chosen against the code |
| MIG-65 | Startup clock-sync gate (regression pin only) | Functional | pytest | dev-PC | n/a | T | P2 | Cite `tests/test_logging.py:723-780` and `tests/test_settings.py:350-360`. Add nothing but a W2025-box rehearsal note: induce skew against a reachable SNTP peer and confirm `time_sync_fail_closed` refuses the start on the real host |
| MIG-66 | `tzdata` is present wherever `zoneinfo` is load-bearing | Compat | pytest + verify | W2025-box | n/a | T | P2 | Windows has no system tz database (`messagefoundry/timezone.py:21-23`). Assert `tzdata` is a resolved dependency in `requirements.lock`, and add a `verify` host row that resolves `ZoneInfo("America/New_York")` and reports FAIL (not SKIP) if it raises `ZoneInfoNotFoundError` |
| MIG-67 | CLI surfaces survive a non-cp1252 console codepage | Usability | pytest | dev-PC | n/a | T | P2 | Extend `tests/test_cli.py:43` beyond top-level `--help`: run `--help` for every subparser, plus `verify`, `check`, `connection schema --json`, and `import corepoint --json` under a forced non-cp1252 encoding with `errors="strict"` on a wrapped stream. Assert no `UnicodeEncodeError` and that `--json` output stays ASCII (`ensure_ascii=True`) |
| MIG-68 | Non-en-US Windows locale cell | Usability | CI-leg | W2025-box | SQLite | T | P2 | One Windows CI matrix cell with a non-en-US system locale and a non-cp1252 OEM codepage: run `verify --section host`, `check`, and the NSSM install/start/stop smoke. Assert no encode error in stdout, in `service.out.log`, or in the `verify` report, and no mojibake in a config path containing a non-ASCII directory name |
| MIG-69 | The support bundle diagnoses a seeded fault | Functional | pytest | dev-PC | x3 | T | P2 | Seed each of the six dispositions (`RECEIVED`, `ROUTED`, `UNROUTED`, `PROCESSED`, `FILTERED`, `ERROR`) plus a dead-letter and a stopped Connection; build the bundle. Assert `manifest.json` + `status.json` + `config-summary.json` + the redacted `app-log.txt` **together** identify the failing Connection name and the failing stage — with **no message body, no secret, and the store path as basename only** (extends `tests/test_support_bundle.py`) |
| MIG-70 | Support triage rehearsal | Usability | manual | any | any | C | P2 | One rehearsal per release: an operator collects a bundle for a seeded fault and a maintainer, given only the zip, names the failing Connection and stage. A failed rehearsal is a finding against the bundle contents, not against the operator |
| MIG-71 | Naive-user INSTALL-GUIDE walkthrough executed verbatim | Usability | manual | W2025-box | SQLite | T | P1 | A first-time operator with no prior Python or engine install follows `docs/INSTALL-GUIDE.md` top to bottom on a clean Windows Server 2025 box, typing only what the document prints. Every deviation, guess, or dead end is recorded as a finding with the line number. PASS = a serving engine, a reachable `/ui`, and one synthetic message through to `PROCESSED`, with **zero** undocumented steps. Repeat from a clean VM snapshot after each doc change |
| MIG-72 | Naive-user USER-GUIDE + EARLY-ADOPTER-GUIDE Stage 0 walkthrough | Usability | manual | W2025-box | SQLite | C | P2 | Same discipline over `docs/USER-GUIDE.md` and `EARLY-ADOPTER-GUIDE.md` §11 Stage 0's four exit criteria. Findings are filed against line numbers. Synthetic corpus only, produced by `messagefoundry generate` |
| MIG-73 | Area go/no-go roll-up is a signed artifact | Compat | manual | any | n/a | C | P1 | A per-release MIG sign-off sheet: every P0 row PASS; MIG-71 executed on the release candidate with zero undocumented steps; MIG-08/MIG-47's downgrade answer recorded in EARLY-ADOPTER-GUIDE §13; the MIG-60 partner checklist filled for every named partner in scope; a named approver accepting residual risk (mirroring **W25:S6.3**'s shape, not duplicating its content) |
| MIG-74 | **Every doc path named in shipped text or a docstring resolves** — one linter, one boundary allowlist | Compat | pytest | dev-PC | n/a | T | P1 | **Owner of the plan-wide "doc paths resolve" deliverable** — WEB-56 and SEC-09 point here and MIG-35 folds in; no separate work is scoped there. One new module walks every repo-relative path reference and markdown link target in `docs/**/*.md`, `README.md`, `CLAUDE.md` and the docstrings/comments of `messagefoundry/**`, `harness/**`, `tee/**`, `scripts/**`, `ide/src/**`, and resolves each on disk. Three outcomes, none of them silence: **RESOLVED**; **WITHHELD** — a path under `docs/security/`, `docs/reviews/` or `docs/marketing/` (gitignored post-cutover, `.gitignore:144-146`) matched against a checked-in manifest of expected names, reported as withheld and never a FAIL in a public checkout — and the manifest cannot rot vacuous (an empty manifest, or a WITHHELD claim for a path *outside* those three directories, FAILS); **BROKEN** — anything else reds the gate (`TEST-ENVIRONMENT-PLAN.md §5`, MIG-42, is today's example). Folds in MIG-35: a `#NNN` BACKLOG reference must resolve to a `## NNN.` section **or** classify as above-baseline, where `NNN > 231` (`docs/BACKLOG.md:6883`) is valid evidence and an unresolved `NNN ≤ 231` is a FAIL. A `file:line` citation is checked for file existence, and where the anchor names a symbol, that the symbol is present |
| MIG-75 | **Non-ASCII patient data survives ingest → store → console → export byte-for-byte** | Functional | pytest | dev-PC | x3 | T | P0 | A synthetic corpus whose PID-5, PID-3 and OBX-5 carry accented Latin (`Zoë Ångström-Muñoz`), CJK (`山田 太郎`) and a declared `MSH-18` character set (one `UNICODE UTF-8` case, one `8859/1` case), driven through every hop and byte-compared at each. **(1) Ingest:** the inbound decodes with the connection's declared `encoding` at `errors="strict"` (`pipeline/wiring_runner.py:3485-3494`) and `normalize()` collapses line endings only (`parsing/peek.py:152-163`); on an `MSH-18`-vs-configured-`encoding` conflict the engine must either honour `MSH-18` or record a **named `ERROR` disposition** — silently decoding under the other charset, or substituting U+FFFD into the stored raw, is the FAIL. **(2) Store:** the raw round-trips identical on all three backends and the at-rest-encrypted body decrypts to the same bytes — SQL Server text columns must be `NVARCHAR`, never `VARCHAR`, since a code-page collation silently mangling CJK is the classic version of this defect. **(3) Console:** `GET /messages/{id}` and the `/ui` render return the same code points (rides the console suite's seam, `packaging/messagefoundry-webconsole/tests/test_webui.py`). **(4) Export/attachment:** the NDJSON from `GET /messages/export` (`api/app.py:3035`, #124 / ADR 0131) and the reconstructed bytes from `GET /messages/{id}/attachments/{attachment_id}` (`:3225`, #149 / ADR 0105) are byte-identical to what arrived, base64 attachment path included (ADR 0028 `mfb64:v1:` — **never** latin-1). Finally the outbound re-encode emits the same bytes. **Gap being closed:** `MSH-18` occurs **nowhere** in the tree (0 hits repo-wide), so the declared character set is never consulted; the only adjacent coverage is `tests/test_mllp_encoding_override.py` (17 tests, delimiter re-encode, accented names incidental) and the IDE hex-pane row (ADR 0119). Synthetic names only |
| MIG-76 | Wall clock stepped **backwards** while lanes drain — no reordering, no duplicate delivery, no stalled lane | HA/Resilience | pytest | dev-PC | x3 | T | P1 | Under a steady synthetic send with all three stages draining, step the wall clock back 90 s (monkeypatched `time.time` in-process; a container clock step on the CI leg) and assert four things. **(a) No reordering:** per-lane delivery order stays strict FIFO. ADR 0059 makes ordering `seq`-only precisely so a backward step cannot reorder — this row is the regression pin for that decision and for the **#285** per-lane-FIFO no-skip conformance gate (above the published #231 baseline); a claim ordered by `created_at, seq` would fail here, which is what makes the row falsifiable. **(b) No duplicate delivery:** each message is delivered exactly once — no in-flight row is re-claimed because a lease or due-time comparison went backwards (`queue.next_attempt_at`, `store/store.py:1183`; `ix_queue_ready`, `:1189`; `reset_stale_inflight`, `:5762`), and the leader lease neither flaps nor is stolen (`pipeline/cluster.py:94`, `lease_expires_at`). **(c) No stalled lane:** a retry stamped before the step still becomes due and every lane reaches `in_pipeline_depth() == 0` within the retry budget instead of waiting out the skew. **(d)** The forward-step direction fires no retry storm and no duplicate delivery. **Leap-second note:** a positive leap second is a repeated second, not a step, so (a)–(c) subsume it — but no assertion in this row (or in the code it guards) may depend on `time.time()` being *strictly* increasing: equal consecutive timestamps are legal and must not be treated as an ordering error |
| MIG-77 | `connections.toml` forward/backward schema compatibility across engine versions | Compat | pytest | dev-PC | n/a | T | P1 | The existing rows test parity (code-first vs data-authored desugaring to identical `Registry` entries); this row tests **versioning**, which nothing does. `connections.toml` carries **no version marker** and its reader is a strict allowlist (`config/connections_file.py` `_INBOUND_KEYS`/`_OUTBOUND_KEYS`, `_reject_unknown` at :300-306) — unlike `connection schema --json`, which does emit `schemaVersion` (`config/connection_schema.py:45`) and which the IDE refuses forward-incompatibly. Pin both directions against a per-vintage fixture set built with MIG-06's generator discipline: **newer file → older engine** — a table carrying a key added after that engine (`flagged`, `deployed`, `auto_start` are the worked examples, `connections_file.py:110-119`) fails loud as a `WiringError` naming the unknown key **and** the file, never a silent drop of the key's effect and never a partial `Registry`; **older file → newer engine** — a table written before those keys existed loads unchanged and desugars into `Registry` entries byte-identical to today's, with the documented defaults (`auto_start`/`deployed` TRUE, `flagged` FALSE), so an adopter's committed file survives an engine upgrade untouched. Additionally assert that every key added to either allowlist since the previous release is named in the upgrade notes MIG-16 checks. If the owner later rules that a version marker is wanted, this row becomes its conformance test; either way the *behaviour* is pinned now |
| MIG-78 | HL7 ACK byte-format compatibility across engine versions, as seen by an unchanged partner | Compat | pytest + CI-leg | dev-PC | n/a | T | P1 | A partner that did not upgrade must keep parsing our ACK. Pin `build_ack` (`transports/mllp.py:329`) against checked-in **golden ACK bytes** for every (`AckMode.ORIGINAL`/`ENHANCED`/`NONE`) × (AA/AE/AR) pair, asserting: MSH-1 and MSH-2 are echoed from the inbound and never hardcoded (`:357-358`); MSH-7 is present and DTM-shaped (an empty MSH-7 makes strict senders NAK-loop and re-send — the regression the default at `:349` exists for, documented at `:343-345`); MSH-10 echoes the inbound control id unless overridden (`:366`); MSA-1 carries the mode-correct code (`AA→CA`, `AE→CE`, `AR→CR` under ENHANCED, `_CODES` at `:325`, applied at `:350`); MSA-3 free text is escaped and every echoed value CR/LF-stripped (segment-injection guard, `:359-366`); and an unparseable inbound still yields a well-formed ACK with the default separators (`:352-355`). The goldens are the contract: any diff in ACK bytes must be an intentional, changelogged change, and the failing test names the golden file so a drive-by edit reds. **N-1 leg:** in the two-venv `store-downgrade` job (which already installs the prior published wheel), generate the prior engine's ACK for the same synthetic inbounds and byte-compare — a difference passes only if it is a declared entry in the compatibility notes. Synthetic inbounds only |

### 17.5 Detailed scenarios

#### S-MIG-A — Store-backend switch with a populated SQLite store (MIG-01, MIG-02)

**Why narrative.** This is the P0 with no code behind it, and getting the fixture wrong turns it into a tautology. The trap is asserting on a *fresh* SQLite file: `open_store` would have nothing to detect either way.

**Preconditions.** A dev PC; `postgres` and `sqlserver` extras installed (or the pytest run parametrized to skip the server legs when the driver is absent, reporting SKIP-with-reason, never pass). A tmp config root holding `messagefoundry.toml`.

**Steps.**
1. Write `messagefoundry.toml` with `[store] type = "sqlite"` and `path = "<tmp>/mefor.db"`.
2. Open the store through `open_store(StoreSettings(...))` ([`store/base.py:1739`](../../../messagefoundry/store/base.py)). Ingest 25 synthetic ADT messages produced by `messagefoundry generate` into the ingress stage without draining, so `await store.in_pipeline_depth() == 25`. Close.
3. Confirm the file exists and is non-trivial (`> 8 KiB`), and re-open/close once to prove the fixture is a *real, migrated* store rather than a blank file.
4. Flip `[store] type` to `postgres` (then `sqlserver`) against the **same config root**, leaving the SQLite file in place, and call `open_store` again with `caplog.at_level(logging.WARNING)`.

**Observation point.** `caplog.records` at WARNING or above, and the raised exception (if the fail-closed posture flag is set).

**Expected result.** At least one record whose message contains the absolute path of the abandoned SQLite store **and** the integer `25`. Under the fail-closed posture flag, `open_store` raises with the same two facts. For MIG-02, drain to `in_pipeline_depth() == 0` first and assert at most one INFO record and no WARNING.

**This test fails today** — `open_store` performs no such detection. It is the executable form of the §17.9 Q4 decision: if the owner rules "documentation-only", the row becomes MIG-03 alone and this scenario is deleted rather than weakened.

**Cleanup.** `tmp_path` teardown drops the SQLite file. Drop the server-DB schema created by the switch (`DROP SCHEMA` in a fixture finalizer) so a re-run starts empty; a leftover populated server schema silently converts a later greenfield assertion into a no-op.

---

#### S-MIG-B — Store vintage matrix, forward and backward (MIG-06, MIG-07, MIG-08, MIG-11)

**Why narrative.** Two engine versions must coexist, which no single pytest process can do. Getting the split wrong produces a test that proves only that the current engine can read its own output.

**Preconditions.** A local wheelhouse holding the last three published engine wheels (or index access). Persistent volumes on the SQL Server / PostgreSQL service containers so a store survives an engine swap. `tzdata` installed.

**Steps — forward (in-process, `pytest`).**
1. Check in a **generator per vintage**, not a binary DB: a small module that emits the exact DDL + a handful of rows for schema vintage V. This keeps the fixture reviewable, keeps PHI out by construction, and survives a repo that cannot carry binaries.
2. For each vintage, materialize the DB, open it with the current engine, and assert: `_migrate` ([`store/store.py:2933`](../../../messagefoundry/store/store.py)) completes; every column named in `_MESSAGE_MIGRATIONS`, plus `audit_log.row_hash`, `audit_log.client`, `users.channel_scope`, `roles.permissions`, is present; each pre-existing message row is fetchable with its original `status`; `in_pipeline_depth()` equals the vintage's queued count.
3. Re-open and assert **zero** DDL executed (probe the `PRAGMA table_info` call count on SQLite; the `schema_meta` marker skip on the server backends).

**Steps — backward (two processes, CI leg).**
1. `python -m venv .venv-cur && .venv-cur/bin/pip install ./dist/messagefoundry-<current>.whl`. Serve, ingest 50 synthetic messages, drain, stop.
2. `python -m venv .venv-prev && .venv-prev/bin/pip install "messagefoundry==<previous>"`. Point it at the **same** store path / connection string. Start.
3. Record the outcome verbatim.

**Observation point.** The prior engine's startup log and exit code; then, if it started, a `GET /messages` read of the 50 rows.

**Expected result.** Exactly one of: (a) clean start with all 50 rows readable at their original dispositions, or (b) a loud refusal naming the schema mismatch and exiting non-zero. A silent partial open, a `NULL`-filled new column read as data, or a re-stamped older `schema_meta` hash that re-runs an older DDL batch is a **FAIL**, and is the answer to §17.9 Q6.

**Cleanup.** Drop and recreate the server schemas between vintages. Delete both venvs. **Never** point this at a store that has ever held real data.

---

#### S-MIG-C — Engine upgrade across a web-console seam bump (MIG-14, MIG-15, MIG-16, MIG-17)

**Why narrative.** The consequence is worse than the recon and the runbooks assume, and the test must observe the *engine*, not `/ui`.

**Preconditions.** A venv with `messagefoundry` and `messagefoundry-webconsole` installed and matching. `[api].serve_ui` left at its default (**true**, `config/settings.py:672`).

**Steps.**
1. Confirm the baseline: `messagefoundry serve --config samples/config --db ./tmp.db --env dev` starts and `/ui` responds.
2. Simulate the skew in-process: monkeypatch `messagefoundry.api._ui_seam.ENGINE_UI_SEAM` to `16` (the console declares `frozenset({15})` at `messagefoundry_webconsole/__init__.py:48`) and call `create_app(..., serve_ui=True)`.
3. Assert `UiSeamMismatch` propagates **out of `create_app`** — not caught, not degraded to a JSON-only app.
4. Assert the message names both integers and the remedy.
5. Independently, parse `packaging/messagefoundry-webconsole/pyproject.toml` and assert `[project].dependencies` carries a bounded `messagefoundry>=X,<Y`.
6. Independently, assert the upgrade sections of `INSTALL-GUIDE.md` §10 and `EARLY-ADOPTER-GUIDE.md` §13 name the console distribution.

**Observation point.** The exception type and message at step 3; the dependency specifier at step 5; the document text at step 6.

**Expected result.** Step 3 passes today. **Steps 5 and 6 fail today** — the dependency is a bare `"messagefoundry"` (`:42`) and neither runbook mentions the second wheel. Step 3's assertion is what makes the runbook wording in step 6 accurate: the operator must be told the engine will not start, not that "the console disappears".

**Cleanup.** None (in-process). If the live `serve` at step 1 was used, stop it and delete `tmp.db`.

---

#### S-MIG-D — Reconcile PHI guardrails at the CLI boundary (MIG-36, MIG-37, MIG-38)

**Why narrative.** Three related defects in one tool, with an ordering constraint: MIG-38 (capture) must land before anyone runs the on-box W25:S3.10 proof, or the proof itself writes a PHI-bearing artifact.

**Preconditions.** No engine required — `harness/reconcile` is offline for `compare`. Synthetic corpus only.

**Steps.**
1. Generate two synthetic HL7 sets, identical except for one field, using needle tokens that appear nowhere else: `ZZNEEDLEFAMILY` in PID-5.1, `ZZNEEDLEMRN` in PID-3.1, `ZZNEEDLEOBX` in OBX-5.
2. Run `python -m harness.reconcile compare --connection IB_TEST --mefor <a>.jsonl --corepoint <b>.jsonl --report-json <out>.json` under `capsys`.
3. Assert **no needle** appears in stdout, stderr, or `<out>.json`.
4. Assert the exit code is non-zero (the pair differs) and the counts are correct.
5. Add `--show-diffs` and assert (a) the needles now appear, and (b) a PHI/TEST-DATA-ONLY warning was written to **stderr before** the first diff line.
6. For capture: start a `CaptureSink` via `python -m harness.reconcile capture --port 0 --out <jsonl> --anonymize --salt-env MEFOR_ANON_SALT` with the salt set, send one needle-bearing message, stop, and assert no needle in the JSONL and no `LeakError`. Then run without `--anonymize` and assert a stderr banner states bodies are written verbatim.

**Observation point.** `capsys.readouterr()` and the on-disk artifacts.

**Expected result.** Steps 3 and 6 **fail today**: `render_text` prints values at `report.py:63-66`, `render_json` always emits `left`/`right` at `:31-42`, and the `capture` subparser has no `--anonymize` flag. The fix mirrors the already-correct `tee` shape (`tee/__main__.py:269`, `:451-453`; `tee/report.py:27`).

**Cleanup.** `tmp_path` teardown. **Never** commit any generated JSONL or report artifact, and never redirect any of these commands' output into a CI log — the same rule that governs `dryrun`/`generate` (CLAUDE.md §9).

---

#### S-MIG-E — End-to-end parallel-run rehearsal (MIG-41)

**Why narrative.** Five processes, an ordering constraint, and a real risk of proving plumbing instead of parity.

**Preconditions.** Two engine instances on non-production ports; the `tee` relay; two capture sinks; a synthetic sender. All loopback. Ports chosen outside the harness's default range so a stray production sender cannot reach them.

**Steps.**
1. Start capture sink A: `python -m harness.reconcile capture --port 2801 --out captures/A.jsonl --anonymize --salt-env MEFOR_ANON_SALT` (per MIG-38).
2. Start capture sink B on 2802 the same way.
3. Start engine 1 (the "incumbent stand-in") with its outbound pointed at sink A.
4. Start engine 2 (the "shadow") with `[shadow].simulate_all_egress` on and its outbound pointed at sink B, so no double delivery can occur (`config/wiring.py:2551-2553`).
5. Start the `tee` relay fanning the synthetic sender to both engines, always-ACK (`tee/relay.py`).
6. Send 200 synthetic ADTs. Wait for `in_pipeline_depth() == 0` on both engines.
7. `python -m harness.reconcile compare --connection OB_TEST --mefor captures/B.jsonl --corepoint captures/A.jsonl --blank MSH-7 --report-json out/rehearsal.json`.
8. Swap engine 2's Handler for one that sets a single extra field; repeat from step 6 into fresh capture files.

**Observation point.** The `compare` exit code at steps 7 and 8; the capture sinks' `captured` / `unparseable` counters; both engines' final disposition tallies.

**Expected result.** Step 7 exits **0** with `matched == 200`, `mefor_only == 0`, `corepoint_only == 0`. Step 8 exits **non-zero** with exactly 200 mismatches on exactly one field. Additionally: both engines report 200 terminal dispositions with zero `ERROR`; the `simulate` outbound still reaches a terminal disposition that correlation can key on — if it finalizes as `FILTERED` and the correlator therefore never sees it, that is the composition defect this scenario exists to find.

**Cleanup.** Stop all five processes. Delete both engines' stores and every capture file. Confirm no listener remains bound on 2801/2802.

---

#### S-MIG-F — Config rollback under load, with dead-letter recovery (MIG-45, MIG-46; step 7 exercises PUB-49's confinement mid-drain, which MIG-48 points at)

**Why narrative.** Destructive, timing-dependent, and the assertion that matters (zero loss) is only meaningful *while* traffic is in flight.

**Preconditions.** A running engine with auth on and a token carrying `config:deploy`. Two config directories inside the allow-listed reload roots: `good/` and `bad/` (whose Handler raises on every message). A synthetic sender at a steady, modest rate.

**Steps.**
1. Serve from `good/`. Start the sender at ~20 msg/s. Confirm dispositions reaching `PROCESSED`.
2. Record `in_pipeline_depth()` and the total ingress count.
3. `POST /config/reload` pointing at `bad/`. Keep sending for 15 s.
4. `POST /config/reload` pointing back at `good/`. Keep sending 15 s more, then stop the sender.
5. Wait for `in_pipeline_depth() == 0`.
6. `GET /dead-letters`, then replay them.
7. Separately, `POST /config/reload` at a path **outside** the reload roots.

**Observation point.** The per-status tallies after step 5; the dead-letter list at step 6; the response and audit row at step 7.

**Expected result.** Total ingress equals the sum of terminal dispositions — no row stuck at `RECEIVED` or `ROUTED`, none missing. Every message sent during the bad window is `ERROR`/dead-lettered, never lost and never silently `FILTERED`. Step 6's replay drives each to `PROCESSED` with the **original raw** preserved. Step 7 is refused with an audited denial naming the acting user, with no partial swap.

**Cleanup.** Stop the engine, delete the store. Run against a throwaway store only — step 3 deliberately creates error rows.

---

#### S-MIG-G — Naive-user INSTALL-GUIDE walkthrough (MIG-71)

**Why narrative.** The only test whose value comes from *not* being automated, and the easiest to run wrong by letting an expert quietly fill gaps.

**Preconditions.** A clean Windows Server 2025 VM with **no** Python, no engine, no NSSM, snapshotted so it can be restored. An operator who has not previously installed this engine. A second observer who records, and never assists.

**Steps.** Follow `docs/INSTALL-GUIDE.md` from line 1, typing only what the document prints, resolving nothing from outside knowledge. When the document is ambiguous, the operator does whatever it most plainly says and the observer records the ambiguity with its line number. Continue to a serving engine, a reachable `/ui`, and one synthetic message through to `PROCESSED`.

**Observation point.** The observer's log: every command that failed, every step requiring an undocumented action, every version literal that did not resolve.

**Expected result.** Zero undocumented steps. Today this **will fail early**: the guide's `pip install "messagefoundry==0.1.0"` (:55) installs a three-releases-stale build, and `:144` describes a `requirements.txt` line that `messagefoundry init` does not write. Both are mechanically caught by MIG-21 and MIG-23 — run those first so the human pass spends its budget on the judgement half.

**Cleanup.** Restore the VM snapshot. Do not reuse a dirtied box for a second walkthrough — residue from the first pass hides the exact class of defect this test exists to find.

### 17.6 Automation disposition

**New pytest modules** (name and scope fixed here so two sessions cannot create differently-named siblings):

| Module | Rows | Effort |
|---|---|---|
| `tests/test_store_backend_switch.py` | MIG-01, MIG-02, MIG-04 | **M** |
| `tests/test_store_vintage_matrix.py` | MIG-06, MIG-07, MIG-09, MIG-10, MIG-11, MIG-12 | **L** — the vintage generators are the bulk |
| `tests/test_docs_adopter_executability.py` | MIG-24, MIG-25, MIG-26 | **M** — port the three proven helpers out of `tests/test_off_loopback_runbook.py` into a shared harness first |
| `tests/test_docs_version_pins.py` | MIG-21, MIG-22, MIG-23, MIG-16 | **S** |
| `tests/test_reconcile_cli.py` | MIG-36, MIG-37, MIG-38, MIG-39, MIG-40 | **M** |
| `tests/test_config_rollback.py` | MIG-45, MIG-46 (+ the mid-drain reload-root case S-MIG-F step 7 — the row itself is PUB-49's) | **M** |
| `tests/test_dialect_corpus.py` | MIG-54 | **M** |
| `tests/test_schedule_dst.py` | MIG-61, MIG-62, MIG-63, MIG-64 | **S** |
| `tests/test_doc_drift_meta.py` | MIG-29, MIG-30 | **S** |
| `tests/test_doc_paths_resolve.py` | MIG-74 (owner: WEB-56 + SEC-09 point here, MIG-35 folds in) | **M** — the withheld-path manifest and the BACKLOG-baseline classifier are the design work |
| `tests/test_non_ascii_end_to_end.py` | MIG-75 | **M** — backend-parametrized ×3; reuses the MIG-06 fixture discipline for the store hop |
| `tests/test_clock_step_back.py` | MIG-76 | **M** — the FIFO/lease assertions need the staged-pipeline fixtures, not a bare store |
| `tests/test_connections_toml_compat.py` | MIG-77 | **S** — per-vintage `connections.toml` fixtures, no DB |
| `tests/test_ack_golden_compat.py` | MIG-78 (in-process half; the N-1 byte-compare rides the `store-downgrade` leg) | **S** |

**Extends an existing module** (do not create a sibling):

| Existing module | Rows |
|---|---|
| `tests/test_feature_map_claims.py` | MIG-27, **MIG-28 (the consolidated FEATURE-MAP drift guard — 15 pointers across the plan land here; one module, no siblings)** |
| `tests/test_install_instruction_provenance.py` | MIG-53 (the widened extra-detector rides TRAY's `[console]`-extra row, which MIG-18 points at) |
| `tests/test_webconsole_seam_snapshot.py` | MIG-14, MIG-15 |
| `tests/test_verify.py` | MIG-17, MIG-66 |
| `tests/test_corepoint_import.py` | MIG-31, MIG-32, MIG-34 |
| `tests/test_connection_schema.py` + `ide/src/test/suite/connection-schema.test.ts` | MIG-19 |
| `tests/test_tee_cli.py` | MIG-43 |
| `tests/test_support_bundle.py` | MIG-69 |
| `tests/test_cli.py` | MIG-67 |
| `tests/test_backlog_status_check.py` + `scripts/docs/backlog_status_check.py` | — (MIG-35's BACKLOG-reference classifier folds into MIG-74's linter, so the boundary is declared once; this module keeps its existing status-banner duties unchanged) |
| `tests/test_fhir_transport.py` | MIG-55 |
| `tests/test_wsdl_import.py` | MIG-59 |
| `tests/test_x12_codec.py` (or the nearest X12 module) | MIG-58 |
| `tests/test_scaffold_requirements.py` | MIG-52 |
| `tests/test_release_pipeline.py` | MIG-53 (release-side half) |
| `tests/test_checks.py` | MIG-52 (report-rendering half) |

**New CI legs** (all gated into `ci-gate`'s `needs:` list unless noted):

| Leg | Rows | Effort |
|---|---|---|
| `adopter-scaffold-e2e` (ubuntu) — `init` → clean venv → wheel install → scaffolded commands → negative case | MIG-50, MIG-51 | **M** |
| `store-downgrade` (container-CI, ×3) — two venvs, prior wheel, shared persistent store | MIG-08, MIG-47, MIG-78 (N-1 ACK byte-compare — the prior wheel is already installed here) | **L** |
| `upgrade-smoke-windows` (W2025 hosted or self-hosted) — N-1 wheel → traffic → N wheel → same store | MIG-49 | **L** |
| `interop` (opt-in, env-gated, skip-loud) — HAPI FHIR + SMART launcher | MIG-56 | **M** |
| `dicom-peer` (container-CI) — dcm4chee/Orthanc service container | MIG-57 | **M** |
| `locale-windows` (one matrix cell) — non-en-US locale + non-cp1252 OEM codepage | MIG-68 | **S** |
| `private-docs` (scheduled, gated) — materialize the **withheld** `docs/security/` set (gitignored post-cutover, not missing) and run the currently-skipped node ids with zero skips | MIG-29 | **M** — depends on §17.9 Q9 |

**New harness / probe capability:**

- `harness/reconcile` gains `--show-diffs` and `--anonymize --salt-env` (MIG-37, MIG-38) — **S**, and these are the prerequisites for W25:S3.10 producing a safe artifact.
- A scripted parallel-run rehearsal under `harness/` (MIG-41) — **L**. It composes existing pieces and must run wholly on loopback with synthetic traffic.
- A synthesized real-grammar Corepoint corpus generator (MIG-31) — **M**. Lives in-repo; the owner's real export never does.
- A `verify` `host.webconsole` seam row (MIG-17) — **S**.

**Stays manual, with the reason:**

| Row | Why it cannot be automated |
|---|---|
| MIG-71, MIG-72 | The judgement "is this instruction followable, and does it mean what a newcomer thinks" is the whole test. The mechanical halves (MIG-21..MIG-26) are automated precisely so the human budget is spent on the judgement half |
| MIG-60, MIG-70, MIG-73 | Domain and accountability judgements — is a partner's quirk "as documented" or their bug; is a bundle diagnostically sufficient; who accepts the residual risk |
| Reconciling the importer against the owner's real Corepoint export | The file carries partner names, site codes, and hostnames and may never be committed, quoted, or pasted (`HANDOFF-105:60-80`). Counts and shapes are read locally, read-only, by the owner. MIG-31 is the in-repo surrogate |
| Owner ruling on which of the ~5,396 unmapped verbs to map vs. permanently decline | The SPEC-105 "considered and rejected" set is a product decision |
| A real cutover reconciliation against a live incumbent on real traffic | W25:S3.10 is explicit that its run is a capability proof on a synthetic golden pair. MIG-41 proves composition; only a real cutover proves parity |
| Interpreting a reconcile diff as a defect vs. a legitimate engine difference | The per-connection blank/sort/ignore rule set is an analyst decision |
| Verifying SLSA/Sigstore attestations with `gh attestation verify` against a real release; the first live PyPI publish; cutting a tag | Network, `gh` auth, a published tag, and owner-only irreversible actions (`docs/releases/v0.1.0-PRETAG-CHECKLIST.md` §C) |
| Confirming a DST transition behaved on a live production schedule | Observable only on the real Sunday. The *simulation* is MIG-61/62/63; the production confirmation is not automatable |

### 17.7 Environment, data & prerequisites

**Hosts and boxes**
- A **clean Windows Server 2025 box** with no prior Python or engine install, snapshotted, for MIG-71/72 and the MIG-49 pinned-wheel upgrade smoke (matches WIN2025-TEST-PLAN **W25:S1.1**'s clean-env prerequisite).
- A **second clean box or VM snapshot** so a walkthrough can be repeated from zero after each doc change without residue.
- NSSM installed, a dedicated service account, and correctly ACL'd store/config/log directories (**W25:S1.AC-ACL**).
- A real desktop session with Chromium + Firefox for the `/ui` operator walk (owned by **W25:S1.AC-DISPO**; cited here only as the prerequisite for MIG-71's `/ui` step).

**Databases**
- SQL Server 2022/2025 and PostgreSQL instances. The existing CI service containers cover the store legs, but the **vintage matrix needs persistent volumes** so a store survives an engine swap (MIG-08, MIG-47).
- A `DROP SCHEMA` fixture finalizer per leg — a leftover populated server schema silently converts a greenfield assertion into a no-op.

**Artifacts to procure or build**
- A **local wheelhouse of the last three published engine wheels** (or index access) for MIG-08, MIG-47, MIG-49. Without it, the entire downgrade/upgrade column is untestable.
- `gh` ≥ 2.49 authenticated, with network egress to GitHub attestations and Sigstore, for the verify-before-install recipe printed at `INSTALL-GUIDE.md:112-114` and `EARLY-ADOPTER-GUIDE.md:192-193`.
- PyPI Trusted Publisher configuration for `messagefoundry`, `messagefoundry-webconsole`, and `messagefoundry-harness` (owner-held; the harness one is the open item behind MIG-53).

**Interoperability endpoints**
- A public **HAPI FHIR** test server (R4B and R5) and the **SMART Health IT launcher** — or an Epic / Oracle Health developer sandbox with registered client credentials and a signing keypair (MIG-55, MIG-56). Requires a §17.9 Q7 ruling before it can be stood up.
- A **DICOM peer** — dcm4chee or Orthanc — with configured AE titles and an explicit accepted-SOP-class list, for MIG-57 beyond loopback `pynetdicom`.
- Synthetic **X12 ISA/IEA envelopes** in partner-specific variants (MIG-58). No trading-partner simulator is required; captured-shape synthetic envelopes suffice.
- An **incumbent engine instance** (Corepoint or Mirth) for a genuine parallel-run rehearsal. Absent one, a second MessageFoundry instance stands in and MIG-41 proves plumbing, not parity — the scenario says so explicitly.

**Clock, timezone, locale**
- `tzdata` installed (Windows ships no system tz database — `messagefoundry/timezone.py:21-23`) and the ability to change the host timezone and system clock for MIG-61..MIG-66.
- An SNTP peer reachable from the box, plus a way to induce skew, for the MIG-65 on-box rehearsal.
- One **non-en-US Windows locale / non-cp1252 OEM codepage** cell for MIG-68.

**Data — synthetic only, no exceptions**
- The primary corpus is `messagefoundry generate` (ADT, ORU, ORM, SIU, DFT, MDM, MFN, OML, ORL, RAS, RDE, VXU, BAR generators under `messagefoundry/generators/`). The generated corpus is git-ignored; **never redirect `generate` or `dryrun` output into a committed file, ticket, or CI log**.
- The needle corpus for MIG-36/37/38/43 uses tokens (`ZZNEEDLE*`) that appear nowhere else in the tree, so a leak assertion cannot pass by coincidence.
- The MIG-54 dialect corpus is hand-authored synthetic HL7 exhibiting *documented structural* quirks. It carries **no partner-identifying names, site codes, hostnames, or MRN formats** — a vendor quirk is a structural property, not a customer artifact.
- The MIG-31 Corepoint corpus is **generated from the SPEC-105 grammar**, not derived from any export.
- Reconcile capture artifacts land on a protected volume with a documented purge, since captures hold bodies unless MIG-38's anonymizer is wired.
- Anything derived from the owner's real Corepoint export is read locally, read-only, by the owner. Counts and shapes only; nothing enters the repo, a ticket, or a prompt.

**Blocked on a ruling before procurement:** the vendor sandbox accounts (§17.9 Q7) and the private-docs CI mechanism (§17.9 Q9).

### 17.8 Exit criteria

This area is signed off for a release when **all** of the following hold and are recorded on the MIG sign-off sheet (MIG-73):

1. **Every P0 row is PASS:** MIG-01, MIG-02, MIG-06, MIG-07, MIG-08, MIG-11, MIG-14, MIG-15, MIG-36, MIG-37, MIG-38, MIG-45, MIG-75. **13 P0 rows, zero waivers.** Only **T** rows gate: the chapter's 7 **C** rows (MIG-33, MIG-56, MIG-60, MIG-64, MIG-70, MIG-72, MIG-73) are recorded on the sign-off sheet as measurements, findings or dated decisions and **cannot** block a release, and there are no **A** rows here.
2. **Zero PHI leaks at the migration-tool CLI boundary.** MIG-36, MIG-37, MIG-38, MIG-43 all pass against the needle corpus, and no default-path invocation of `reconcile compare`, `reconcile capture`, or `tee compare` emits a message body.
3. **The greenfield-only constraint is either enforced in code (MIG-01/02 green) or the §17.9 Q4 decision is recorded** as documentation-only with MIG-03 green and the risk explicitly accepted by a named approver. "Undecided" is not an exit state.
4. **The store vintage matrix is green in both directions** for the last three releases on all three backends (MIG-06 through MIG-12), and MIG-08's downgrade answer is written into `EARLY-ADOPTER-GUIDE.md` §13 as the supported behaviour — replacing today's "not trivially reversible" hand-wave.
5. **Every version literal in shipped text resolves** (MIG-21, MIG-22, MIG-23 green): zero occurrences of a `messagefoundry==X.Y.Z` that is not `__version__`, and zero `messagefoundry-webconsole==X.Y.Z` that is not the console's `__version__`.
6. **Doc-executability is green** across `INSTALL-GUIDE.md`, `USER-GUIDE.md`, `EARLY-ADOPTER-GUIDE.md`, `ADOPTER-CI.md`, `DEPLOY-SERVER-DB.md`: every fenced `toml` block loads with no silently-ignored key, every `connections.toml` block loads through the strict loader, and every printed `messagefoundry <sub> <flags>` invocation exists (MIG-24, MIG-25, MIG-26).
7. **The naive-user walkthrough (MIG-71) has been executed on the release candidate** on a clean box, with **zero** undocumented steps recorded. A walkthrough executed on an earlier RC does not carry forward across a doc change.
8. **The upgrade path is proven end to end:** MIG-49's N-1 → N Windows smoke is green against a single store, and the console wheel is named in both upgrade runbooks (MIG-16) with a bounded PEP 508 range shipped (MIG-15).
9. **Rollback is rehearsed:** MIG-45 shows zero loss under load, MIG-46 recovers every stranded dead-letter, and reload-root confinement holds mid-drain — the row is **PUB-49**'s (MIG-48 points at it); this chapter's obligation is only that S-MIG-F step 7 ran with traffic in flight.
10. **The adopter CI contract is executed, not merely parsed:** MIG-50 green and MIG-51's negative case reds as expected.
11. **Zero silent skips:** the suite reports no unexplained module-level skip (MIG-30), and either the private-docs leg runs with zero skips or §17.9 Q9 is answered and the ~100 assertions inert in a public checkout (≈88 threat-model + the 12 off-loopback runbook tests) are explicitly accounted for. Their documents are **withheld, not missing** (§17.1), so "the file isn't there" is never the accounting.
12. **The FEATURE-MAP drift guard is green (MIG-28):** no ✅ row names a path that does not exist, every ✅ row has an owning test, no 🔨 row names a shipped-and-CI-exercised artifact — and no adopter guide denies a registered route (MIG-27). Because 15 rows across the plan point at MIG-28, a red here is a plan-wide red, not a MIG-local one.
13. **Interoperability posture is stated honestly:** MIG-54's dialect corpus is green, MIG-55's conformance probe is built, and for every partner in the release's declared scope the MIG-60 checklist row is filled — or the partner is explicitly out of scope for that release. An empty interop matrix with a "supports Epic" claim anywhere in shipped text is a **blocker**.
14. **A named approver has signed MIG-73**, accepting the residual risk of every P1 row not closed. MIG-73 and MIG-60 are **C** rows: they gate the *sign-off ritual*, not the test gate — an unsigned sheet means the release is unapproved, never that a test failed.
15. **Every doc path resolves or is declared withheld (MIG-74):** zero BROKEN paths across `docs/**`, `README.md` and the `messagefoundry/**` / `harness/**` / `tee/**` / `ide/src/**` docstrings; a non-vacuous withheld manifest covering the three gitignored directories; and every BACKLOG reference either resolving inside the published baseline or classified above #231.
16. **Character-set fidelity is proven end to end (MIG-75, P0):** accented Latin and CJK patient data arrive, persist, render and export byte-identical on all three backends, and an `MSH-18`-vs-configured-encoding conflict produces a named `ERROR`, never a silent substitution.
17. **Version-seam compatibility beyond the store:** MIG-77 pins `connections.toml` in both directions (an unknown key fails loud on an older engine; an older file loads unchanged on a newer one) and MIG-78's ACK goldens are unchanged — or a changed byte-shape is a declared, changelogged compatibility entry. MIG-76 (backward clock step: no reordering, no duplicate delivery, no stalled lane) is green on all three backends.

### 17.9 Open questions

1. **Is `messagefoundry import corepoint` an adopter-facing capability or an internal migration aid?** `docs/AI-OFF-MATRIX.md:21` marks it "✅ shipped"; `docs/BACKLOG.md:4390` keeps #105 OPEN at P3 demand-gated; `docs/FEATURE-MAP.md` has **no row for it at all**. *Blocks:* the testing bar for MIG-31 through MIG-34, which differs by an order of magnitude between the two answers, and whether the importer appears in FEATURE-MAP at all (MIG-28).
2. **What is the decision on BACKLOG #313 (the multi-message Handler model), and is Corepoint verb coverage in scope for any release?** ~2,032 statements are refused for want of it (`HANDOFF-105:24-28`); #313 sits above the published #231 baseline (`docs/BACKLOG.md:6041`), which makes it sound evidence a public-only reader cannot open, not a doubtful citation. *Blocks:* whether MIG-31's pinned ledger is a *floor that must rise* or a *permanent cap*, and whether the SPEC-105 "specified but unbuilt" items (MIG-34) are scheduled or declined.
3. **Is Mirth import ever in scope, or is the AI chat participant (`ide/src/chat.ts:40`, `ide/package.json:584-585`) the permanent answer?** This chapter's scope names "Corepoint/Mirth import"; only Corepoint exists as a deterministic path. *Blocks:* whether a MIG row for a Mirth importer belongs in the matrix at all, and whether shipped text may say "Mirth migration" without qualification.
4. **Should the greenfield-only SQLite→server-DB constraint be ENFORCED — refuse or loudly warn on a backend switch with a populated prior store — or remain documentation-only?** *Blocks:* MIG-01 and MIG-02 (a code change plus a test) vs. MIG-03 alone (a doc-drift test plus a recorded risk acceptance). This is the single highest-value decision in the chapter.
5. **What is the supported engine↔console version relationship: strict lockstep as coded (`SUPPORTED_ENGINE_SEAMS == frozenset({15})`), or an N-1 range?** The constant's own comment says re-widening requires "the CI matrix that installs the MIN and MAX supported engine builds — the claim and its test land together, or not at all." *Blocks:* whether MIG-15's PEP 508 range is `>=X,<X+1` or a real window, and whether MIG-16's runbook wording is "upgrade both wheels together" or "upgrade the engine, then the console".
6. **Is engine downgrade (pin-back) a supported operation against a store the newer engine has already opened?** `EARLY-ADOPTER-GUIDE.md` §13 prescribes it as the primary rollback lever *and* warns that store-level changes are not trivially reversible. Those two statements cannot both be the guidance. *Blocks:* MIG-08 and MIG-47's PASS definition, and the rewrite of §13 that exit criterion 4 requires.
7. **Which vendor endpoints justify a live interop leg — Epic sandbox, Oracle Health, a named PACS, a named HIE — and does the project have or want the accounts?** *Blocks:* MIG-56 and MIG-57's procurement, and whether MIG-60's per-partner checklist has an executor or is an unfilled template. Without a ruling the interoperability matrix is a checklist with nobody to run it.
8. **Should the adopter guides pin an exact version at all, or say "the latest release"?** `SUPPORT-POLICY.md:9` supports latest-only; the guides teach exact pinning; `INSTALL-GUIDE.md:144` additionally misdescribes what the scaffold writes. Auto-updating a literal on every release is trivial; deciding the policy is not. *Blocks:* whether MIG-21 asserts equality with `__version__` or asserts the *absence* of a literal, and whether MIG-23 exists.
9. **Do the `docs/security`-anchored doc-drift suites need to run in CI, and by what mechanism — private submodule, encrypted checkout, or a dedicated runner?** The documents are **withheld, not missing** (gitignored post-cutover, `.gitignore:144`), so ~88 threat-model assertions and all 12 off-loopback runbook tests are inert in every *public* checkout, CI included — the guard exists and has nothing to read. *Blocks:* MIG-29, MIG-30, and the `private-docs` CI leg.
10. **Where should the reconcile harness's design document live?** Four in-repo modules cite `TEST-ENVIRONMENT-PLAN.md §5` and it exists nowhere in the tree. *Blocks:* MIG-42 — either a synthetic in-repo version is published, or the four citations are re-pointed.
11. **Is a UAT sign-off artifact required per adopter — a filled matrix with a named approver — and does the project ship a template, or is `EARLY-ADOPTER-GUIDE.md` §11's checkbox list the whole thing?** *Blocks:* MIG-73's shape and whether it is a project deliverable or an adopter's own internal document.
12. **Should `verify` grow a web-console seam row, and should its retired PySide6 check be removed or repurposed?** `check_console_importable` (`verify/checks.py:180`) probes a retired surface and names a `[console]` extra that does not exist in `pyproject.toml`. *Blocks:* MIG-17 here, and TRAY's `[console]`-extra / `check_console_importable` row (which MIG-18 points at) plus a correction to `docs/testing/VERIFY.md:40`.
