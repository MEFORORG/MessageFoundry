# HANDBACK — C7: is the store's ceiling partly self-inflicted intra-query PARALLELISM?

**Date:** 2026-07-12 · engine build `98bec81` (pinned) · engine m7i.4xlarge, store i4i.2xlarge (`n_sched=8`) · feature ON

## 0. Verdict (one line)
**Hypothesis KILLED — parallelism EXONERATED, twice over.** Forcing serial plans (DB-scoped MAXDOP=1) made
N=8@3 **materially WORSE** (49.4% → 20.6% delivered, slope +115 → +155) — i.e. **parallelism was HELPING**, not
walling — and it also **BROKE the healthy N=8@2 rung** (100% PASS → 75.7% FAIL). `CXSYNC_PORT`'s 34× growth in C6
was a **collapse EFFECT, not a cause** (the C4 `list_fifo_lanes` trap, confirmed). **Do NOT adopt MAXDOP=1 — it is
harmful.** The `txn/event` levers remain the path (C7 does not displace them). A clean, successful falsifier run.

## 1. Feature + rig
`SERVERPROPERTY('IsTempdbMetadataMemoryOptimized') = 1` verified; `tempdb_xtp` @25%; build `98bec81` (no engine
change); engine m7i.4xlarge, store i4i.2xlarge, `dm_os_schedulers VISIBLE ONLINE = 8`.

## 2. Store parallelism settings AS FOUND (§2.4) — one is non-default (flagged)
- **Instance `max degree of parallelism` = 8 — NOT the default 0.** ⚠️ Literally non-default, but on this 8-scheduler
  box it is **functionally equivalent to 0** (both permit up to 8-way parallelism), so it does **not** change how C6
  reads: parallel plans were enabled and used (why `CXSYNC_PORT` appeared). The intervention (MAXDOP=1) is still a
  real change from effective 8-way → serial, so the A/B is valid.
- `cost threshold for parallelism` = **5** (low default). DB-scoped MAXDOP (`mfbench`) = **0** (default → inherits 8).
- **Restored:** DB-scoped MAXDOP reverted to **0** + procedure cache cleared, verified, after the run.

## 3. MANIPULATION CHECK (§5, blocking) — PASS, the run is valid
`CXSYNC_PORT` `d_resource_ms` (fenced, exclusion-set-filtered): **C7-base = 135,361 → C7-dop1 = 0** (also 0 on
C7-dop1-pass). It collapsed to 0 (< 5% bar = 6,768). MAXDOP=1 took effect — no parallel plans, no exchange. **Valid.**

## 4. Arms (900 s soaks; gate on `result`)

| arm | N/shard | MAXDOP | `result` | deliv % | stranded | slope | store CPU% | eng max_core% | runnable med/max | SOS_YIELD tasks | CXSYNC_PORT | WRITELOG sig-frac |
|---|---|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| **C7-base** | 8@3 | default(8) | SOAK_NOT_SUSTAINED | **49.4** | 87,656 | **+115.1** | 67→81 | 36 | 5/11 | 622,888 | 135,361 | 0.50 |
| ⭐ **C7-dop1** | 8@3 | **1** | SOAK_NOT_SUSTAINED | **20.6** | 137,194 | **+154.7** | 49→**89** | 32 | 10/19 | 748,325 | **0** | 0.54 |
| **C7-dop1-pass** | 8@2 | **1** | **SOAK_NOT_SUSTAINED** | **75.7** | 28,106 | +38.4 | 53→74 | 33 | 4/8 | 536,195 | 0 | 0.50 |

Load-gen CPU <2% every arm (never the limiter). C7-base reproduced the 3-run N=8@3 baseline (49.4%/+115 vs
50–52%/+108–112.5) — **no drift**, so the A/B is against a valid same-session control, not a historical number.

## 5. §6 verdict (pre-registered)
- **C7-dop1 = "materially WORSE"** (delivered 20.6% < 45% AND slope +154.7 > +125) → **PARALLELISM WAS HELPING.**
  MAXDOP=1 hurts; the store *benefits* from parallelism at this load (it drove store CPU higher — 89% — for *less*
  delivery, with more `SOS_SCHEDULER_YIELD`). The hypothesis is exonerated and the config change is warned against.
- **C7-dop1-pass (N=8@2) FAILED the PASS bar** (75.7% delivered, not drained, stranded 28k — vs 100% PASS at
  default). → **CONFIG IS BROADLY HARMFUL** — it breaks even the currently-healthy rung. Not adoptable, full stop.
- **No reproduction arm run** — C7-dop1-rep is mandatory only before a *positive* claim; this is a negative result.

## 6. One-line read
**The store's ceiling is NOT a parallelism config default.** `CXSYNC_PORT` is a collapse effect, not a cause —
naming it the wall off its rank/growth would have been the C2/C4/C5-§6 error, and the A/B refutes it directly.
Parallelism is load-bearing here; removing it makes the collapse worse and breaks the healthy rung. One fewer open
thread — the programme proceeds to the `txn/event` levers (Phase-3 `accepts=`, Phase-4 group-commit), unchanged.

## 7. Sources
Per-arm `out/c7-*/`: throughput `c7-*.json`, `c6_convoy_*.json` (fenced filtered wait delta incl. `CXSYNC_PORT`),
CPU telemetry. Baseline: `HANDBACK_C5_2026-07-12.md` §2 + `HANDBACK_C6-LIVE_2026-07-12.md` §2. Read-only DMV /
public catalog names only; no secrets, IPs, hostnames, ports, or PHI.
